# 掼蛋游戏规则（本仓库实现口径）

本文档从当前代码导出，描述的是本训练器实际执行的规则，不是泛化的民间变体。主要依据：

- `js/cards.js`：牌张、级牌、逢人配
- `js/rules.js`：牌型、比较、升级、过 A
- `js/game.js`：发牌、出牌圈、接风、进贡还贡、整场胜负
- `js/rules.test.js`、`js/game.test.js`：已固化的边界用例

策略、AI 评分和教练建议不属于硬规则，不写入本文。

---

## 1. 对局结构

- 使用两副扑克，共 **108 张**：每种花色点数各 2 张，另加大王 2 张、小王 2 张。
- 四人同桌，对家为队友：
  - 座位 0：南，真人（你）
  - 座位 1：东，下家 AI
  - 座位 2：北，对家队友 AI
  - 座位 3：西，上家 AI
  - 队伍：`0 + 2` 对 `1 + 3`
- 每人发 **27 张**。
- 整场从双方打 **2** 开始，打到一方在本方 **A** 级过关为止。
- 每一副有一个当前级牌。首副打 2；此后由上一副头游方的当前级数决定本副打几。

点数编码：`2` 到 `A(14)`，小王 `16`，大王 `17`。花色：`S` 黑桃、`H` 红桃、`D` 方块、`C` 梅花。

---

## 2. 级牌与逢人配

- **级牌**：本副正在打的点数（任意花色，不含王）。
- 单张、对子、三张、三带二、炸弹比较时，级牌大于 A、小于王：
  - 大王 > 小王 > 级牌 > A > K > … > 2
- **逢人配**：红桃级牌（两张）。可替代任意非王牌。
- 逢人配 **不能** 替代大小王。
- **单张逢人配只能当它自己的级牌打**，不能把一张红桃级牌声明成别的点数。
- 顺子、三连对、钢板里，级牌按自然点数占位，不按“级牌威权”插到 A 之上。
- 非当前级牌的红桃不是逢人配。例如打 3 时，♥7 只是普通 7。

纯逢人配的对子 / 三张 / 炸弹可以声明为任意自然点数；未指定声明时，默认解释为本局级牌。跟牌时自动采用“刚好能压住”的最小声明。

---

## 3. 合法牌型

只承认下列牌型。没有更长顺子、飞机（超过钢板的连续三张）、四带二等。

| 牌型 | 张数 | 构成 | 比较键 |
|------|------|------|--------|
| 单张 | 1 | 任意一张 | 单张威权 |
| 对子 | 2 | 同点两张；两张小王或两张大王也是对子 | 点数威权 |
| 三同张 | 3 | 同点三张 | 点数威权 |
| 三带二 | 5 | 三张 + 一对，主点不同 | 三张的点数威权 |
| 顺子 | 5 | 连续 5 张不同点，每点 1 张 | 最高点 |
| 三连对 | 6 | 连续 3 个对子 | 最高点 |
| 钢板 | 6 | 连续 2 个三同张 | 最高点 |
| 炸弹 | 4–10 | 同点至少 4 张 | 先比张数档，再比点数 |
| 同花顺 | 5 | 同花色的 5 张顺子 | 最高点 |
| 天王炸 | 4 | 两张大王 + 两张小王 | 最大 |

### 3.1 顺子 / 三连对 / 钢板的连续范围

A 可作低位 `1` 或高位 `14`，但不能绕接（没有 `QKA23`、`KA2`）。

顺子只允许：

- `A2345`
- `23456` 到 `10JQKA`

三连对只允许：

- `A23` 对
- `234` 对到 `QKA` 对

钢板只允许：

- `A2` 三张
- `23` 三张到 `KA` 三张

### 3.2 逢人配多解

同一组实体牌可以同时存在多种合法声明。例如打 9 时，`3/4/5 + 两张逢人配` 可声明为：

- 顺子 `A2345`
- 顺子 `23456`
- 顺子 `34567`

若这 5 张同花，上述三个声明还会各自保留对应的同花顺。

两对加一张逢人配，可分别声明成以其中任一点为主的三带二。

`33/44 + 两张逢人配` 可声明 `223344`、`334455` 两个三连对，以及 `333444` 钢板。

### 3.3 同花顺不可降级

一组牌只要已经构成某条同花顺，领出或跟牌时 **不能** 再把它按普通顺子结算。逢人配补进同花色缺口时也一样。例如打 2：`♥2 + ♣10♣J♣Q♣A` 必须按 A 高梅花同花顺处理。

### 3.4 王的限制

- 王不能进入普通炸弹、顺子、三带二、三连对、钢板。
- 两张相同的王可以组成对子。
- 逢人配不能和王配成对子或三张。

---

## 4. 大小比较

比较函数是 `canBeat(后手, 上手)`：必须 **严格大于** 才能压，同点同型不能压。

总阶：

1. **天王炸** 大于一切。
2. **炸弹类**（炸弹、同花顺）大于一切普通牌型。
3. 炸弹类内部按内部 `power` 比：
   - 6 张及以上炸弹
   - 同花顺
   - 5 炸
   - 4 炸
4. 同档炸弹再比点数威权（级牌炸弹大于同张数的 A 炸）。
5. 普通牌型必须 **同类型且同张数**，再比 `power`。
   - 对子不能压单张
   - 钢板不能压三连对
   - 6 张炸弹可以压钢板，因为炸弹属于炸弹类

同花顺与炸弹的分界：

- 任意 4 炸、5 炸都压不过同花顺
- 任意 6 炸及以上都压过同花顺

跟牌未指定声明时，优先采用 **同型且刚好能压住** 的最小解释；其次才用更大的炸弹类。领出未指定声明时，采用可比较意义上更强的解释（同花顺优先于普通顺子）。

---

## 5. 出牌流程

- 轮转顺序：南 → 东 → 北 → 西（座位号 `0 → 1 → 2 → 3`）。
- **领出** 必须出一组合法牌型，不能过牌。
- **跟牌** 必须压过上手，或选择过牌。有牌可压时也可以主动过。
- 上手出牌后，其余仍需表态的在局玩家都过后，由最后出牌者继续领出。
- 剩余张数第一次降到 **10 张及以内** 时公开报牌一次。
- 某人出完后记入名次：头游、二游、三游、末游。

---

## 6. 接风（借风）

核心约定：**刚出完的那一手，对家不参与压制，只等待接风。**

1. 某人打出最后一手后，其对家暂时不是跟牌人。
2. 只需另一队仍在局的玩家表态。
3. 若这两人（或剩余对手）都过，则由对家 **接风领出** 新的一轮。
4. 若有人压住了这手牌，接风取消，牌权转给压牌者；原本等待接风的对家恢复为普通跟牌人。
5. 若对家也已经出完，则由顺时针下一位仍在局的玩家领出。

双上（头游、二游同队）时本副立即结束，不再让剩下两人互相比出三、四名。未出完者按当前座位之后的顺时针顺序补记为三游、末游，只用于复盘和下一副进贡数据。

已有三人出完时，第四人自动记为末游。

---

## 7. 升级

按头游方对家的名次加级，加在 **头游那一队** 的级数上：

| 结果 | 名次 | 升级 |
|------|------|------|
| 双上 | 头游 + 二游同队 | +3 |
| 头三 | 头游 + 三游同队 | +2 |
| 头末 | 头游 + 末游同队 | +1 |

- 级数顺序：`2 → 3 → … → K → A`。
- 加级封顶为 A，不会越过 A。
- 已经在 A 的一方，若本副打的是对方的级牌，即使获胜也只保持 A，不能直接过关。

---

## 8. 打 A 与整场胜负

过 A 只属于 **本副级牌的归属方**（正在打 A 的那一队）。

必须同时满足：

1. 本副当前级牌是 A，且这 A 是该队自己的级数。
2. 该队取得头游。
3. 对家不是末游，也就是双上或头三。头末不能过 A。

失败处理：

- 本方正在打 A，但没有取得头游 + 二游/三游：本副过 A 失败，失败次数 +1。
- 连续 **3 次** 在本方 A 级未过关：该队打回 2 级，失败次数清零。
- 对方在此期间仍可按普通规则升级。

只有过 A 成功才会结束整场。

---

## 9. 进贡、还贡、抗贡

从第二副开始，发牌后根据上一副名次处理贡还。首副随机一名玩家先出，不进贡。

### 9.1 抗贡

进贡方（上一副输的一方）合计持有 **两张大王** 时抗贡：

- 单下：只看末游是否有两张大王。
- 双下：看两名输家合计是否有两张大王。

抗贡后本副不进贡、不还贡，由上一副头游先出。

### 9.2 进贡

进贡牌为该进贡人手中 **非逢人配** 的最大牌（按本副级牌威权）。两张同样大时，实现里再按结构保护做选择。

- **单下**（头游与二游不同队）：末游向头游进贡一张最大牌。
- **双下**（头游与二游同队）：两名输家各进一张最大牌；较大的那张给头游，较小的给二游。

进贡牌面公开。

### 9.3 还贡

收到进贡的一方必须还一张牌给原进贡人。

还贡限制（训练口径）：

1. 若手中有 **点数 ≤ 10 的非级牌、非王**，必须从这些牌里还。
2. 否则只能还当前最小牌。优先在非王、非逢人配中取最小；没有这类牌时才从整手牌取最小。同大小可任选。

### 9.4 贡还后的首出权

- 单下：由进贡的下游（末游）先出。
- 双下：由进贡牌更大的那一家先出。
- 抗贡：由头游先出。

---

## 10. 本实现与常见竞赛口径的差异

下列条目是代码里明确采用或简化过的口径，阅读外部规则书时不要直接替换：

1. **顺子固定 5 张**，没有 6 张及以上顺子。
2. **同花顺不可声明为普通顺子**。
3. **进贡不选逢人配**，除非手里只剩逢人配。
4. **双上后立刻结束**，三、四名按顺时针补齐，不再实打。这不影响升级（都是 +3），也不改变双下“大贡给头游、小贡给二游”的分配方式。
5. **首副随机先出**，不按红桃 3 或其他固定亮牌规则。
6. **过 A 绑定本副级牌归属**。已到 A 的一方不能借打对方级牌的双上直接获胜。
7. 还贡的“≤10 非级牌”是硬约束，界面只会高亮这些牌。

---

## 11. 规则在代码中的入口

| 规则 | 函数 / 位置 |
|------|-------------|
| 逢人配判定 | `cards.js` → `isWild` |
| 单张威权 | `cards.js` → `soloPower` |
| 多解识别 | `rules.js` → `parseHandVariants` |
| 能否压牌 | `rules.js` → `canBeat` / `isLegalPlay` |
| 合法出牌生成 | `rules.js` → `generateLegalPlays` |
| 升级 / 过 A | `rules.js` → `describeUpgrade` / `canPassA` |
| 进贡还贡 | `game.js` → `setupTribute` / `getReturnCandidates` |
| 接风 | `game.js` → `pendingWindPartner` / `advanceAfterPass` |
| 局末结算 | `game.js` → `endRound` |
