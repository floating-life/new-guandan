# AGENTS.md — new-guandan AI 开发协作规则

本文件定义 `new-guandan` 项目中 Codex 主代理、子代理、模型选择、推理强度、测试、审查与外部 Grok 审计的默认工作方式。

目标：在保证代码正确性、掼蛋规则正确性、AI 公平性和项目稳定性的前提下，尽可能节约 ChatGPT Plus / Codex 额度。

除非用户在当前任务中明确覆盖，本文件为项目默认规则。

---

## 1. 总原则

1. 用户只需要描述业务目标，不需要逐项指定模型、思考等级或子代理。
2. 主代理负责自行完成：
   - 需求理解
   - 仓库调查
   - 架构设计
   - 任务拆分
   - 子任务派发
   - 实现
   - 测试
   - 修复
   - 内部审查
   - 外部审查材料准备
   - 最终验收
3. 模型选择遵循：**使用能够可靠完成任务的最低成本模型 + 最低合理推理强度**。
4. 不要因为节约额度而降低：
   - 规则正确性
   - 数据正确性
   - AI 公平性
   - 用户历史数据兼容性
   - 回归测试质量
   - 核心算法可靠性
5. 不要把所有工作都交给最强模型。
6. 不要为了省额度而强制让弱模型先失败一次；任务明显复杂时直接升级。
7. 尽量复用已经完成的代码调查、测试结果和上下文，避免多个代理重复扫描整个仓库。
8. 只有在产品需求存在无法合理推断且会导致明显不同实现方向的重大歧义时才询问用户。模型选择、子代理选择、推理等级不得作为向用户追问的理由。

---

## 2. 默认模型组织结构

### GPT-5.6 Sol — 主代理 / 架构师 / 最终裁决者

默认职责：

- 项目级任务理解与调度
- 架构设计
- 关键数据模型
- 复杂算法
- 跨模块技术取舍
- 疑难问题分析
- 最终发布审查
- 对外部 Grok 审查意见作最终裁决

默认推理强度：`Medium`。

升级到 `High` 的情形：

- 新架构或跨模块重构
- AI 核心决策算法
- 牌型、升级、贡还、借风等规则核心
- 数据模型迁移或历史数据兼容
- 并发、状态机、恢复机制等复杂问题
- 连续修复失败的疑难 Bug
- 最终发布审查
- 判断外部 Grok 审计意见是否成立

最高等级推理默认禁止常驻使用。

只有满足以下至少一种条件时才允许临时升级到当前环境可用的最高推理等级：

- Sol High 已无法可靠解决；
- 同一复杂缺陷已经经过有证据的多轮修复仍失败；
- 涉及非常高复杂度且错误代价高的核心算法或架构决策。

Sol 不应执行：

- 大量 grep / 文件枚举
- 重复读取整个仓库
- 样板代码
- 简单格式化
- 常规 lint
- 普通测试执行
- 明显机械性的批量修改

---

### GPT-5.6 Terra — 主力工程师

默认职责：

- 普通前端开发
- 普通后端开发
- Python 服务代码
- JavaScript 业务代码
- 页面与交互实现
- localStorage / 本地数据管理
- API 开发
- 状态管理
- 一般重构
- 中等复杂度 Bug 修复

默认推理强度：`Medium`。

升级到 `High` 的情形：

- 复杂业务逻辑
- 多模块联动
- 较难状态 Bug
- Medium 已经有明确证据不足以可靠完成

Terra 是正式编码工作的默认主力，不要无理由把普通开发升级到 Sol。

---

### GPT-5.6 Luna — 执行工程师

默认职责：

- 文件定位
- 仓库调查
- grep / 搜索
- 找调用关系
- 简单代码阅读与摘要
- lint / 格式检查
- 编译或启动检查
- 运行测试
- 补普通单元测试
- 样板代码
- 小修改
- 明确规则下的批量修改
- 简单重构
- 机械性验证

默认推理强度：

- 搜索、定位、测试、格式检查：`Low`
- 小修改、样板代码、补测试：`Medium`

Luna 不承担：

- 项目级架构决策
- 核心掼蛋规则裁决
- AI 公平性边界设计
- 高风险数据迁移设计
- 最终发布裁决

如果任务明显超出 Luna 能力，直接升级 Terra，不要连续重复尝试消耗额度。

---

## 3. 自动升级规则

不要机械地让每个任务都从 Luna 开始。

按任务性质直接路由：

```text
机械、确定性工作         -> Luna
普通工程实现             -> Terra
复杂业务/复杂缺陷         -> Terra High
架构/关键算法/重大决策     -> Sol High
极端复杂且 High 已失败     -> Sol 最高可用推理等级
```

失败后的升级顺序应基于失败原因，而不是无脑逐级重试。

典型升级路径：

```text
Luna Low
  -> Luna Medium（仅当任务仍属 Luna 范畴）
  -> Terra Medium
  -> Terra High
  -> Sol High
  -> Sol 当前环境最高等级（极少使用）
```

如果从一开始就能判断属于架构或核心算法问题，可直接使用 Sol High。

---

## 4. Codex Plus 额度策略

软目标，不是硬配额：

- Luna：约 50%–60% 的任务数量
- Terra：约 30%–40% 的任务数量
- Sol：约 5%–10% 的任务数量

注意：这里指任务数量，不代表 token 或实际额度比例。

应优先把高频机械工作下沉，把最贵模型的调用保留给最重要的决策。

### 上下文节约

1. 子代理优先读取与当前任务直接相关的文件。
2. 不要多个代理分别完整扫描整个仓库。
3. 主代理先让一个低成本调查代理建立“项目地图”，后续代理复用该结果。
4. 修改后审查优先基于：
   - 原始需求
   - 实施方案
   - git diff
   - 修改文件
   - 测试结果
   - 已知限制
5. 除非确有必要，不要为了审查重新读取全部历史文件。

---

## 5. new-guandan 项目硬约束

这些约束优先级高于模型成本优化。

### 5.1 AI 公平性

AI 不得读取任何其他玩家尚未公开的真实手牌。

允许使用：

- AI 自己的手牌
- 已公开出牌
- 公开余牌数量
- 贡牌 / 还贡信息
- 玩家顺序
- 历史公开行为
- 基于公开信息建立的概率模型
- 公平采样的假想信息集

禁止：

- 直接读取真人或其他 AI 的真实暗牌用于决策
- 通过调试状态、全局对象、存档字段等旁路偷看暗牌
- 为了让 AI 更强而破坏信息集公平性

局末亮牌或完整牌局记录可以用于复盘、训练数据生成和离线评价，但不得反向影响当时实时决策。

### 5.2 掼蛋规则

以下属于硬规则层，策略和模型不得覆盖：

- 合法牌型
- 牌型大小
- 逢人配解释
- 接风 / 借风
- 升级规则
- 进贡 / 还贡
- 出牌合法性
- 公开信息边界

AI 策略只能在合法动作集合内部选择。

### 5.3 用户历史数据兼容

本项目大量状态保存在浏览器 `localStorage`。

涉及以下内容时必须考虑旧数据迁移与兼容：

- 设置
- 战绩
- 统计
- 玩家画像
- 复盘
- 进行中牌局
- AI 分析数据
- 数据导入 / 导出

新增字段优先使用向后兼容的可选字段或带版本的数据结构。

不得无提示清空用户已有数据。

涉及存储格式变化时必须：

1. 明确旧版本格式；
2. 设计迁移或安全默认值；
3. 测试旧数据加载；
4. 测试新数据保存后再次加载；
5. 测试 JSON 导入 / 导出兼容性（若相关）。

### 5.4 本地优先

项目当前定位为本机掼蛋训练应用。

除非用户明确要求，不要擅自：

- 引入云账号体系
- 上传用户牌局数据
- 引入不必要的外部服务
- 把本机私有数据暴露到 LAN / Internet

---

## 6. 标准任务工作流

对于中大型功能，默认使用以下流程：

### Phase A — 调查

优先：`Luna Low`

目标：

- 定位相关文件
- 找入口与调用关系
- 找现有数据结构
- 找测试位置
- 找既有实现模式
- 输出简短项目地图

不要在这一阶段大规模改代码。

### Phase B — 架构判断

只有确实需要架构决策时使用：`Sol High`

输出：

- 数据结构
- 模块边界
- 兼容策略
- 风险点
- 实施顺序
- 验收标准

简单功能无需为了形式专门调用 Sol High。

### Phase C — 实现

默认：`Terra Medium`

可把相互独立的模块并行派给多个 Terra，但必须避免同时修改高冲突文件。

机械性配套工作可派给 Luna。

### Phase D — 测试

优先：`Luna Medium`

至少验证：

- 目标功能
- 原有功能回归
- 错误输入
- 边界条件
- 数据持久化
- 旧数据兼容（若相关）
- AI 公平性（若相关）

复杂测试失败由 Terra 分析；只有疑难核心问题再升级 Sol。

### Phase E — 内部审查

先使用低成本静态检查和测试证据。

仅在功能达到候选发布状态后，由 `Sol High` 做最终内部审查。

Sol 审查重点：

- 需求是否真正完成
- 是否有隐藏的数据破坏
- 是否破坏 AI 公平性
- 是否引入规则错误
- 是否存在回归
- 是否存在不必要复杂度
- 测试证据是否足够

---

## 7. Grok 4.6 外部独立审查

Grok 4.6 的角色是：**外部红队 / 独立审计员**。

Grok 不参与项目日常指挥，不拥有最终裁决权，也不应直接控制代码修改。

Codex 当前任务完成候选版本后，应准备 `external-review-package`，供用户交给 Grok 4.6 审查。

除非用户明确要求，不要因为外审自动提交额外文件到仓库；可以在任务结果中生成可复制的审查包内容。

### external-review-package 至少包含

1. 原始需求
2. 实施摘要
3. 关键架构决策
4. git diff 或明确的改动列表
5. 修改涉及的关键文件
6. 测试命令与测试结果
7. 已知限制
8. 重点希望 Grok 挑错的位置

### 给 Grok 的默认审查任务

Grok 应主动寻找：

- 需求遗漏
- 错误假设
- 架构缺陷
- 数据一致性问题
- 状态问题
- 边界条件
- 安全问题
- 性能退化
- UI / UX 问题
- 测试覆盖盲区
- “看似完成但实际未完成”的功能
- 为通过测试而形成的脆弱实现
- AI 是否通过旁路读取了不应读取的暗牌

问题分级：

- P0：阻止发布
- P1：严重问题
- P2：应修复
- P3：优化建议

每个问题应尽量包含：

- 文件 / 代码位置
- 问题描述
- 为什么是问题
- 触发条件
- 如何验证
- 推荐修改方向

无充分证据的问题标为“待验证”。

外审结论使用：

- PASS
- PASS WITH FIXES
- BLOCK

---

## 8. Grok 审查后的处理

收到 Grok 审查结果后：

1. 不得无条件接受 Grok 的全部建议；
2. 由 `Sol High` 结合：
   - 原始需求
   - 实际代码
   - 掼蛋规则
   - 测试证据
   - Grok 论据
   进行裁决；
3. 成立的问题再派 Terra / Luna 修复；
4. 不成立的问题应说明驳回理由；
5. P0 / P1 修复后必须重新测试；
6. 必要时重新形成增量外审包，而不是再次发送整个仓库。

原则：**Grok 有质疑权，没有指挥权；Sol 有最终技术裁决权。**

---

## 9. 测试与验收要求

修改前先识别项目已有测试方式，不要凭空引入新的测试框架。

当前仓库已有 Python 服务测试文件（如 `lan_server.test.py`）以及前端 / AI 相关现有测试与工具时，应优先复用。

每次任务应尽可能执行与改动最相关的测试，而不是只声称“应该可以”。

如果由于当前环境限制无法运行某项测试，必须明确写出：

- 未运行什么
- 为什么不能运行
- 已完成哪些替代验证
- 用户或后续代理应如何验证

不得把“未运行测试”描述成“测试通过”。

---

## 10. Git 与并行代理规则

1. 多代理并行时优先使用独立 worktree / branch，减少相互覆盖。
2. 不要让两个代理同时大范围修改同一核心文件。
3. 主代理负责整合并解决冲突。
4. 不得覆盖用户尚未提交的本地修改。
5. 发现工作区已有用户修改时，先识别和保护，不要 reset、checkout 覆盖或强制清理。
6. 禁止为了让测试通过而删除未知来源的用户修改。
7. 在没有明确授权的情况下，不执行破坏性 Git 操作。

---

## 11. 当前 Codex 不支持显式模型路由时的降级规则

如果当前 Codex App / CLI / 子代理运行时不能显式指定 Sol、Terra、Luna 或 reasoning effort：

1. 不要因此中断任务；
2. 保留本文件的职责分层思想；
3. 用当前可用模型完成工作；
4. 尽量让机械任务使用低成本 / 低推理代理；
5. 把高推理预算集中到架构和最终审查；
6. 在最终报告中说明哪些路由规则因运行时限制未能严格执行。

不得假装已经使用了实际上无法选择的模型。

---

## 12. 最终汇报格式

完成任务后，向用户报告：

1. **实现内容**
2. **关键架构决策**
3. **主要修改文件**
4. **测试结果**
5. **兼容性 / AI 公平性检查**
6. **Grok 外审待审内容或 external-review-package**
7. **已知风险 / 后续优化项**
8. **模型与推理路由摘要**
   - 实际使用了哪些模型
   - 实际使用了哪些推理等级
   - 为什么这样路由
   - 是否因运行时限制发生降级

不要把大量低价值过程日志堆给用户；优先报告决策、证据、结果和风险。

---

## 13. 一句话执行原则

> **Sol 管理与裁决，Terra 负责主力开发，Luna 承担高频执行，Grok 负责体系外挑错；用满足可靠性的最低成本模型完成每一步，把高推理额度留给真正重要的问题。**
