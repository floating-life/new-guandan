# 掼蛋训练大师：整体项目路线图

> 审核基线：2026-09-03，当前 checkout 分支 `main`、HEAD 为 0903 的 STRAT-2 翻转提交（此前 `810b24e` 审计收尾、`1d66d75` 字节冻结修复），已推送并通过远端 verify CI run [`33703113243`](https://github.com/floating-life/new-guandan/actions/runs/33703113243)（3m40s）；EVID-9b 冻结提交 `585f099` 已完成默认/完整数据验证并通过远端 Windows CI（run `33470270032`）。STRAT-5/RT 残余与 STRAT-6/REL 已随 `c7e4c9b` 入库，工作树除执行日志草稿（不入库）外干净；0903 独立审计：工作树 22/22 评测闭包文件 SHA 与 STRAT-6 正式报告一致、该提交树上统一验证通过，提交 blob 因 `core.autocrlf` EOL 归一导致字节冻结未达成的问题已由 `1d66d75` 修复（blob 聚合复现 `94cccadd…`）。
> 当前发布结论：**专家策略继续作为默认（含 0903 晋级翻转为 true 的 STRAT-2）；没有 `promoted` 模型；`ismcts-v3` 不晋级；STRAT-3/4 开关保持 `false`。**
> 当前主阶段：**R0 证据门加固（EVID-9b 代码/远端 CI 已闭环，但真实发布证据未形成）+ R1A 实时复盘智能体桥（RT-1～RT-6 本地代码门完成，训练准入未完成）+ R1B 复盘驱动的专家策略修复（STRAT-1～6 正式镜像已收割，候选仍关闭）+ R1 专家版脚本化封版（真人手打与设备卡顿未测）+ R2 搜索尾延迟诊断**。`tools/extracted/` 已被 Git ignore，本机原始混合来源导出已删除，用户亦确认 WPSDrive 同步历史/回收站已按策略清除。DMC-0 格式硬门已关闭；ALGO-2 首出保护代码门与 TEL-1 预算遥测语义已关闭，PERF-2b 大 checkpoint 诊断已完成，但不改变当前性能/强度停止线；在真实发布证据、性能和强度门闭环前，不启动新的正式长跑、force-expert 臂或 DMC/DanZero 训练。真人对局复盘在 R1A 训练转换门完成前一律 `trainingEligible=false`；STRAT-2 数字门通过并经 0903 只读晋级审查复算属实，字节冻结阻断已由 `.gitattributes` closure-eol 修复（提交 blob 22/22 复现 `94cccadd…`），按用户授权完成翻转进入 expert 默认（残余风险与不适用声明见 todo.md 0903 台账）。

本文是项目目标、阶段依赖和硬门的当前总纲。近期可执行工作见 [todo.md](./todo.md)，既往实现与实验流水见 [PROJECT_EXECUTION_PLAN.md](./PROJECT_EXECUTION_PLAN.md)。本机 `data/` 产物默认被 Git 忽略，引用时必须同时给出文件名、SHA-256、冻结配置和结论。

## 一、0902 决策快照（承接 0901）

| 路线 | 当前事实 | 当前阻断 | 决策 |
|---|---|---|---|
| R0：证据可信度 | EVID-1～9b、SEC-1～3、UI-0 与 VERIFY-1 本地门已关闭；本机原始导出已删除并被 ignore，用户确认同步副本已处置，抽取器只接受显式 localhost origin + 固定回放键，`-FullData` 已与连续赛报告解耦；冻结提交 `585f099` 的默认 **38 checks**、`-FullData` **42 checks** 及远端 Windows CI run `33470270032` 已通过；0902 迁移收尾恢复了被目标外改动删除的 `tools/test_ai_ab_simulation.mjs`（当时闭包 21 配套），STRAT/RT 线与 TEL-1/PERF-2b/ALGO-2 线合并后的工作树统一入口先通过 **42 checks**，RT-4 后为 **43 checks**，RT-5 后为 **45 checks**，本轮 RT-6 后复验为 **46 checks**，评测闭包现为 22 个源码文件 | 真实 M2 checkpoint、raw telemetry、正式性能回执、连续赛和盲评工件仍不存在；当前 v3 性能门仍失败；本轮未运行 `-FullData`、远端 CI 或发布晋级验证 | R0 代码/CI 子门关闭；保持 expert 默认，待 RUN-2～6 形成真实发布证据 |
| R1：规则/专家版 | 规则、专家策略、Worker、浏览器主流程和自动回归主体完成；DMC 假选项已移除，旧设置迁移为 expert；README 已收敛；复盘正确标注 ISMCTS v3/成对 sweep 且不进选择器；脚本化三副与静态隐私/回滚台账已写入 `data/expert-release-acceptance.json`；0903 真人手打实测（笔记本 + 手机视口 390×844，Chromium ~89 FPS、P95 帧间隙 12ms、共 4 次 0.3-0.9s 停顿、内存峰值 48MB）写入 `data/expert-release-interaction-measured.json`，并运行时抽查本源 localStorage 无 API Key | 真机触控/真机 GPU 未测（视口模拟）；大师难度卡顿轮廓未测 | 专家版脚本化封版+实测可回滚；不得把内嵌 Chromium 视口实测写成真机/大师难度证据 |
| R1A：实时复盘智能体桥 | `trickLog` 已逐手记录动作、圈号、余牌数、评价和决策元数据；公开/密封/annotation 三层契约 41/41；真实动作边界发出 play/pass/trick_end/round_end；本机队列默认关闭网络提交；opt-in 回环采集、短期读 token、cursor/短轮询、容量/保留期 fail closed；只读消费者与副末密封批次不可训练；RT-6 回环 HTTP 与一次真人长局公开链；本轮关闭 Infinity/嵌套崩溃、GET 限流、once 分页、annotation 半行截断、upgradeCode 交叉校验、跨座位牌张唯一和恢复断链提示；统一入口 **46 checks** | 密封批次无刷新持久化；进行中存档仍含四家暗牌，不能作为智能体数据源；append 仍全量扫描日志；`trainingEligible=false` | 判定为**本地代码门满足、训练准入未满足**。RT-1～RT-6 代码门完成；实时智能体永不读取暗牌，真人轨迹默认不可训练 |
| R1B：复盘驱动专家策略 | STRAT-1～5 代码门完成；STRAT-6 正式三臂已在实现 SHA `94cccadd…`（= 评测时工作树闭包）、种子 `20268111–20268150` 收割：STRAT-2 数字门通过，STRAT-3 CI/灾难失败，STRAT-4 与 expert 完全等价 | 0903 字节冻结修复（`.gitattributes` closure-eol）后，STRAT-2 按审查条件与用户授权翻转进入 expert 默认；STRAT-3/4 保持关闭；内节点 STRAT-4 仍读根 `publicHistory`（P2-a 夜间修复）；原始 0831 复盘已删除 | STRAT-2 已翻转、STRAT-3/4 保持关闭；翻转后发布证据须与新提交重新互绑，不宣称 promoted/releaseEvidenceReady |
| R2：PIMC / ISMCTS | availability-aware UCT、评测对手画像隔离和 1800 节点 v3 已实现；PERF-1 历史分层诊断、PERF-2a opt-in 环境遥测短 smoke、PERF-2b 隔离大 checkpoint fresh/resume 诊断、ALGO-1 深层事务回滚、ALGO-2 rollout 首出不变量与 TEL-1 预算语义闭包均完成 | 完整运行的搜索 P95/P99 为 `619/1022ms`，超过 `500/750ms`；大 checkpoint 诊断显示恢复/保存会增长但未形成历史尾延迟根因，Windows 无 loadavg/热或云盘同步观测 | 保持专家默认；禁止在该批种子继续 fxe 或据正 CI 晋级；PERF-3 仅在根因有证据时实施 |
| R3：DMC / DanZero | 有公平自对弈、32 维线性实验模型及 `guandan-env-v1` 交换格式；DMC-0 现严格拒绝非有限数、非法牌张/转换计数、重复 seed、坏 SHA-256 与畸形 JSON，并将 `training/*.py` 纳入统一语法门 | 仍缺独立 Python 规则环境、10 万条零差异语料、PyTorch DMC、ONNX 和可加载权重 | 正式训练继续暂停；下一项是 DMC-1 的独立差分环境 |
| R4：本机对手建模 | v3 公开行动画像、100 副半衰期和 `off / observe / adaptive` 已实现 | 缺分布漂移检测、个性化假想发牌权重和独立留出收益门 | 保持安全候选内的小幅软调整，不扩大作用域 |

## 二、不可绕过的硬门

### 0. 本地数据与凭据安全

- 浏览器存储抽取只能按明确 origin 与 `guandan_*` 键写出最小值，不得因一条 LevelDB write batch 含 `guandan` 就保存整批字节。
- 浏览器原始导出、回放和分析结果不得放在云同步仓库或进入 Git 候选快照；抽取目录必须被忽略，正式需要保留的最小化回放另走已审计的脱敏数据路径。
- 一旦混合来源导出包含第三方控制台 origin 与令牌类标记，先撤销/轮换可能受影响的会话并处理同步副本，再删除本地原始导出；完成前禁止提交、推送和对外分发。

### 1. 证据完整性

- 正式 A/B 必须预登记候选、对照、源码完整依赖闭包、解析后的搜索配置、运行时版本、机器信息、种子清单和报告路径。
- checkpoint 必须原子写入并可校验恢复；resume 前后不得混合源码、配置、对手画像模式或模型摘要。
- 全部 80 个基础牌区组 × 13 级 × 双腿必须完成，即 2,080 局 / 1,040 镜像组；失败、死锁、镜像不一致均为 0。
- 所有 AI 决策都进入遥测分母。缺失 `localDecision` 必须计作 `unmeasured`，不得直接丢弃后得到 100% 覆盖率。
- 正式种子一次预登记、永久留痕；烟雾、探针、调参或失败运行用过的种子不得再次冒充未见正式种子。

### 2. 强度门

- 以基础牌区组为独立重采样单元；`candidatePairedUtilityBootstrap95` 下界必须严格大于 0。
- 点估计为正、头游略高、小样本胜率或不同报告拼接均不能替代正的配对置信下界。
- 任一级牌、计划局数、种子 provenance、模型语义摘要或连续赛回执缺失时不得晋级。

### 3. 性能门

- 只按真实 `searchTriggered` 决策判定：至少 100 个触发回合、测量覆盖率 ≥99%、P95≤500ms、P99≤750ms、可判定超时回退率 <0.5%。
- 正式回执中整体分布和每个运行段的 `searchTriggered` 原始五项计数必须严格守恒；每个运行段都独立满足上述固定门。不得用未触发搜索的快回合、早期快段或单独探针稀释慢段。
- 机器信息必须由评测进程写入并与报告互绑，不能由事后汇总机器代填。

### 4. 真人盲评与消融门

- 盲评至少 10 名参与者、每人至少 10 题、100% 完成、无重复分配；参与者聚类 bootstrap 95% 下界严格大于 0.5。
- 灾难性错误必须对每道题的 expert/proposed 两侧全量复核并用同一题目分母比较；未被玩家选择的一侧也不能漏检。
- fxe 仅接受 v3 checkpoint、`opponentModelMode=off`、完整依赖摘要和完全相同的搜索配置/区组。强制臂相对 expert 的效用、头游、双上必须逐镜像组严格等价，正常 `changed` 与强制 `wouldChange` 必须逐对局对象一致。
- fxe 是机制归因，不能抵消正常臂的强度或性能失败。

### 5. 发布门

- 发布验证命令只有在 `promotion.promoted=true` 且全部必需回执互绑时才能返回 0；`validated` 或 CI 下界不正必须返回非 0。
- M2 需要一个统一、只读的发布验证器，同时核验正常臂完整性、强度、性能、源码/配置/机器 provenance 和盲评回执。
- 正式发布还需在精确提交树上通过本机验证与远端 CI，并记录版本、已知边界和回滚方式。

### 6. 实时复盘与训练隔离门

- 智能体实时读取的 `guandan-live-public-event-v1` 只能包含已公开动作、当前圈号、四家公开余牌数、公开贡还、执行引擎/版本和脱敏决策元数据；不得读取 `saveActiveMatch` 的完整状态、四家初始牌或任何尚未公开手牌。
- 事件必须在实际动作提交后生成稳定 `eventId` 与单调 `sequence`，覆盖 `play / pass / trick_end / round_end`；重试可重复发送但服务端必须幂等，游标读取能检测丢号、重复和跨副串线。
- 实时采集默认关闭并由用户显式启用。服务仅绑定 `127.0.0.1`，写入限同源页面，智能体读取使用短期 capability token；数据写入批准的非同步本机目录，不进入仓库、WPSDrive 或浏览器 LevelDB 原始导出。
- 未来训练使用独立的 `guandan-sealed-training-turn-v1`：只保存行动座位自己的手牌、当时公开观察、完整合法候选、唯一实际动作、规则/源码摘要与同副结果。该密封流在副末前不得暴露给实时智能体。
- 真人轨迹、智能体复盘意见和外部轨迹默认 `trainingEligible=false`。智能体意见作为引用 `eventId` 的独立 annotation，不得自动成为动作优劣标签；只有经过用户同意、规则重放、牌张守恒、候选唯一性、去重和按整场切分审计的批次才可单独批准训练。

### 7. 复盘规则晋级门

- 真实复盘发现先固化为只含行动者手牌和公开历史的最小反例；检测器命中是风险线索，不等于真实失误率。原始工件不存在时必须标为不可复算历史摘要。
- 每条新规则必须有独立 `policyFeatures` 开关、默认关闭，并由 AI、教练、搜索和云端增强共用同一 eligible-action/invariant 层；禁止在多个评分点重复拍权重或在每个引擎复制规则。
- 单元测试必须同时覆盖命中反例、方向/座位旋转、边界张数、合法回退和不应命中的负例。单测全绿只证明机制，不构成默认晋级。
- 每条规则单独做未见种子镜像消融并报告团队效用、名次、灾难率和性能；失败或证据不足保持关闭。三个 AI 位置继续共享座位/队伍规范化策略，只在评测中按座位、角色、先手和贡还分层。

## 三、阶段路线

### R0：证据可信度加固（代码/CI 子门已闭环，真实发布证据未形成）

目标：先消灭能够产生假绿的路径，再消费新的正式种子和计算时间。

必须完成：

1. [x] 修正 `validate_release_evidence.mjs`：CI 下界不正时不得把 `promotion.ok=true` 当作可发布，退出码必须与 `promotion.promoted` 对齐。
2. [x] 修正 A/B 遥测分母：所有 AI 决策均记录；缺元数据计未测量并使覆盖率下降。
3. [x] 将评测指纹扩展为可审计的运行依赖闭包。清单覆盖状态机、Worker、统计/存储、评价、环境遥测与全部策略依赖，并回归“间接依赖变化后 resume 被拒绝”。
4. [x] checkpoint 采用同目录临时文件、`fsync`、重读全量校验和不删除目标的原子替换；`.last-valid` 仅保留已验证版本，恢复还核精确对象覆盖与 pair↔game/failure 一致性。
5. [x] 性能回执绑定评测机和运行段；汇总器按本机字节重算源码清单，硬校验全 13 级、`opponentModelMode=off`、候选与 `searchMode` 一致，并令每段 `searchTriggered` 五项原始计数与总体守恒。
6. [x] 修正盲评全题灾难门：summary v3 按 manifest 全部题目分别统计 expert/proposed，使用共同场景分母；未选 proposed 灾难、缺复核、重复 ID 与数量不一致均有回归并阻断。
7. [x] 收紧 fxe 契约：拒绝 legacy v1/v2、模式不一致和缺失指纹，并只接受 v3 checkpoint。
8. [x] 建立 M2 统一发布验证器和负例测试。`tools/validate_m2_release.mjs` 互绑主 A/B、checkpoint、原始遥测、严格性能回执、当前源码连续赛和真人盲评的场景载荷/灾难复核；盲评 assignment/mapping 摘要按冻结场景、顺序和随机种子确定性重建，答案 ledger 逐题核对 `side === mapping[choice]`；`tools/test_validate_m2_release.mjs` 覆盖完整正例以及 checkpoint、遥测、性能分段、连续赛、盲评 allocation/gate/catastrophic、来源/实现摘要和跨零 CI 的缺失/篡改/矛盾负例，均要求非 0。`verify.ps1` 已显式传入 selected scenarios 与 catastrophic review。**本地代码门已关闭，真实发布证据仍待 RUN-2～6。**
9. [x] 完成 EVID-9 同一提交闭环：候选提交 `585f099` 已在独立脱离工作树通过默认 **38 checks**、`-FullData` **42 checks**、A/B checkpoint 原子/恢复及环境遥测回归，并通过远端 Windows CI run `33470270032`；当前工作树仍确认 `git ls-files -u` 为空、工作区/staged 差异格式通过。真实 M2 工件仍未形成，不得把本轮未冻结 STRAT 改动混入该 EVID-9b 证据。

退出条件：上述负例均有自动回归；索引无冲突且依赖完整；默认验证、完整数据验证和远端 CI 必须在同一干净提交上通过。EVID-9b 的代码/远端 CI 子门已满足；真实发布证据仍由 RUN-2～6 负责，expert 默认不变。

### R1：专家版发布闭环（可并行）

目标：先把不依赖实验搜索的稳定专家版做成可回滚基线。

- [x] 脚本化三副验收：普通升级、贡还、打 A / 不过 A（easy 档状态机，非真人手打）。
- [x] 静态隐私/回滚台账：默认离线、回环云端、采集器关闭、`trainingEligible=false`、停止服务 / 改回专家 / 清 localStorage + git 回退。笔记本与手机卡顿未测。
- [x] 网页默认仍是 expert；`ismcts-v3` 仅离线评测，复盘标签为 ISMCTS v3/成对 sweep，不进入产品选择器。

### R1A：实时复盘智能体桥（新增，可与 R1/R2 并行）

目标：让本机智能体按每手、每圈和每副近实时读取可审计复盘，同时为以后再训练保留严格隔离、可验证的原始轨迹。

1. [x] 定义公开事件、密封训练 turn、智能体 annotation 三个独立 schema；`js/replay-contracts.js` 统一绑定 match/round/trick/turn、规则版本、实现摘要和事件链摘要，严格拒绝暗牌/训练标签字段，密封 turn 固定 `trainingEligible=false`；公开决策元数据只保留受控 token，未知值归一为 `unknown`，实现摘要绑定完整契约序列化依赖清单；`js/replay-contracts.test.js` **41/41** 通过并覆盖链连续性、内容摘要复算、annotation 源摘要绑定与 fail-closed。
2. [x] 在 `applyPlay` / `applyPass` 成功及圈/副结束的提交边界接入只读事件观察器，不从 UI render 回调取数；公开 projector 用白名单构建事件，不保留实体牌 ID 或副本索引，`js/replay-event-queue.js` 以有界本机队列持久化并默认关闭网络提交；HTTP 业务拒绝、无身份或错配回执不推进游标，结束事件拒绝动作载荷，完整链首事件强制从 `sequence=0` 开始；刷新后保留的溢出或一般完整性缺口（包括旧记录漏写标记时由链本身发现的缺口）锁存并继续 fail closed；无可用持久化存储时明确标记非 durable 并阻断提交；覆盖真人、三个 AI、贡还、接风、双上提前结束和打 A 终局。队列回归 **39/39**，观察器回归 **12/12**，完整一副集成回归通过。
3. [x] `lan_server.py` 增加 opt-in 本机采集：浏览器幂等 POST，智能体按绑定 `matchId` 的 `afterSequence` 分页或最多 5 秒短轮询 GET；提供同源、严格公开事件/摘要/链校验、限流、请求体上限、短期 `X-Guandan-Replay-Capability` token、NDJSON 追加事务失败回滚与 `fsync`、按日/容量轮转、保留期和磁盘满安全降级。清理造成链缺口、嵌套实体身份字段和跨语言数字摘要均 fail closed；`/api/replay/status` 与 UI 状态胶囊暴露缺口，页面按服务端状态启停提交，采集失败不阻断出牌；`lan_server.test.py` **39/39**，当前统一入口 **42 checks**。
 4. [x] 提供只读 CLI/智能体适配器持续消费事件并把分析写入单独 annotation；`tools/replay_consumer.mjs` 严格限制回环 GET 和 CLI/环境变量 token，按 `afterSequence` + `matchId` 续读，复核公开契约、摘要、连续序号和前序摘要，网络/响应/存储异常保留已确认 cursor 并 fail closed；以原子 `cursor.json` 和独立 0600 `annotations.ndjson` 持久化，输出每手、每圈、每副聚合复盘，annotation 记录模型、提示版本、创建时间和源事件摘要，不含源事件副本、暗牌、收益或训练标签。`node tools/test_replay_consumer.mjs` **20/20**。
5. [x] 副末将密封 turn 与真实名次/团队收益连接；`js/sealed-training.js` 验证规则合法性、公开观察边界、候选集合和唯一 chosen 后生成 `guandan-sealed-training-batch-v1`，`tools/sealed_training_converter.mjs` 去重并按完整 match 切分 train/validation/held-out。批次仍保持 `trainingEligible=false`，经人工批准后才进入训练清单；公开观察器和进行中存档不携带密封内容。`node js/sealed-training.test.js` **32/32**，转换器回归 **8/8**，统一入口 **45 checks**。
6. [x] 完成端到端安全与完整性本地代码门：`tools/test_replay_e2e.mjs` 经真实回环 HTTP 写入一副收束对局的公开事件，两个 `replay_consumer.mjs` 从中途 cursor 续读；公开流无暗牌，密封批次可转换，`validate_dataset_manifest` 拒绝 `trainingEligible=false`。合成负例覆盖暗牌注入、跨 origin、过期 token、超大请求、重复幂等、磁盘满和保留期删链。UI 采集控制提供暂停/恢复/清空待发，状态含最后序号、保留期和智能体连接。启用仍须启动脚本 opt-in。`lan_server.test.py` **47/47**，队列 **41/41**，UI 静态 **47/47**，统一入口 **46 checks**。

退出条件：两台本机消费者断点续读不丢不重；实时流暗牌泄漏负例、跨 origin/坏 token/超大请求/磁盘满/重试幂等均有回归；回环 HTTP 完成一副公开事件后，密封批次可离线重放且未获批准时训练器明确拒绝。本地代码门已满足；浏览器真人长局、密封刷新持久化和训练准入仍未满足，不得宣称轨迹可训练。

### R1B：复盘驱动的专家策略修复（新增，可与 R0/R1A 并行）

目标：在不放松发布门的前提下，优先修复真实上桌暴露的专家策略低级失误，并防止规则叠加造成优先级冲突。

1. [x] 把 0831 的报单送单、抢队友牌权和名次目标案例转换为公开信息最小夹具；夹具只保留行动者手牌、公开历史、合法候选和预期不变量，并绑定规则版本与实现摘要。历史统计因原始导出已删除只作线索，未来由 R1A 自动留存可复算反例。
2. [x] 当前“长手保留 A/级牌三张或三带二”修复已挂独立开关 `reserveHighControlLead`，统一 AI、共享策略与教练按出牌后剩余张数 `>8` 的阈值，消除同一概念多处叠罚；两手收官保持明确豁免。STRAT-6 正式臂数字门通过；0903 字节冻结修复后按审查条件与用户授权翻转进入 expert 默认（baseline 保持 false）。
3. [x] 已建立面向所有活跃对手的报单领牌安全约束：`enemyReportLeadSafety` 默认关闭，仅在完整合法候选存在不拆高资源的非单或 `public_lock_lead` 路线时过滤 J 及以下低单；无替代动作、整手收官和跟牌分支不触发。领牌解释、云端咨询与混合搜索均使用过滤后的候选，旧式低单首选也不能借本地锚点回流。队友已锁定本圈的优先级不变量仍由 STRAT-4 单独处理。
4. [x] STRAT-5：expert、root PIMC、ISMCTS v2/v3 根候选和云端咨询子集只消费 `filterEligibleStrategyActions`；本家内节点同样过滤，对手内节点与 rollout 不套用本家 STRAT-3/4。`ai.test.js` **369/369**，`ai.hybrid.test.js` **105/105**。候选仍默认关闭。
5. [x] STRAT-6：每条规则独立开关、独立镜像消融、未见种子 `20268111–20268150`；继续使用一套座位/队伍规范化策略。正式三臂均完成 520 对、零失败；STRAT-2 数字门通过（0903 审查复算属实；字节冻结未达成，不翻转）；STRAT-3/4 未过门。贡还层不可识别；先手角色/队伍分层来自 checkpoint 局对象，不作新角色特征。

退出条件：每个候选规则单独达到预登记镜像门、不增加灾难性错误，**并且**证据绑定同一 git 提交，才可逐项进入 expert 默认。STRAT-1～6 评测已收割；STRAT-2 经 0903 审查与字节冻结修复后已翻转进入 expert 默认（残余风险已披露），STRAT-3/4 保持关闭。

### R2：搜索尾延迟诊断与重新晋级

依赖：R0 完成。

1. [x] 用现有 checkpoint 建立 legacy 字段可复算的按区组、每 10 区组、1–60/61–80、座位、遥测来源、级牌和 candidateTeam 分层只读分析器。`tools/analyze_statefix_performance.mjs` 总体 search-triggered 6677 条、P95/P99 `619/1022ms`，1–60 为 `5058`、`421.8/550ms`，61–80 为 `1619`、`942/1481ms`。该历史 v2 完成项不包含内存/GC、CPU/电源或真实进程/resume 标签；v2 没有内生运行段或环境指标，外部 sidecar 仅标 `externally_declared/provenanceVerified=false`，因此仍不是正式性能门证据。新 v3 运行环境接线见 PERF-2a，真实大 checkpoint 对比仍待 PERF-2b。
2. [x] 接通 opt-in 运行环境遥测并完成短 fresh→中断→resume smoke：每段记录 RSS/heap、GC、checkpoint 构建+校验、序列化、写入/fsync、临时重读+校验、备份/rename及总耗时、进程/系统 CPU、频率、电源和外部负载；Windows `os.loadavg()` 无效时以明确标注的 `os.cpus()` 时序/频率作代理。sidecar 与核心工件路径冲突前置拒绝，状态/摘要/计时由内容复算；正式性能汇总器和 M2 明确拒绝 diagnostic-only `environmentTelemetry`。`tools/test_environment_telemetry.mjs`、A/B smoke 和缺段/篡改回归已通过。
3. [x] 已完成隔离的大 checkpoint 诊断：按预登记在 `20260941–20260980`、`ismcts-v3` 对 `expert`、全 13 级/level-block 下，于 20 区组 24,685,236 字节 v3 checkpoint 外部中断后 resume 至 40 区组。恢复严格使用 primary，读/解析/校验 `48.737/72.232/125.245ms`（`246.278ms`）；fresh/resume 各 520 局，最终 47,851,028 字节、零失败/死锁/镜像偏差，运行段/sidecar 为 complete。保存成本随对象增大（末次 `918.683ms`→`1,657.466ms`，峰值 RSS/heap `716/528MiB`→`1,355/1,052MiB`），但样本环境与工作负载不同：仅有 Windows CPU 代理 `49.641%/41.635%` 和 active scheme，无有效 loadavg、温度或云盘同步 I/O。因此这只排除了“没有当前 v3 大 checkpoint 事实”的证据缺口，不能推出历史 P95/P99 的根因、不能启动 PERF-3 优化，也不构成性能/强度/晋级证据。
4. [x] 修正 v3 失败 sweep 的深层事务语义：每个 sweep 暂存整棵开放环树；中断或后置候选 rollout 失败时恢复深层 availability、子节点、visits/reward、outcomes/failures 与终端/截断计数。opt-in digest 回归先形成成功深树，再注入后置失败并继续成功 sweep，证明失败不改变后续可见树。
5. [x] 固定 rollout 首出不变量：`lastHand=null` 时非空手牌不得返回 `null/pass`；牌型生成器异常只剩非收官炸弹候选时，确定性回退到最小结构成本合法着法并记录 `lead_no_ordinary_fallback`。回归覆盖正常首出、7/9 张只返回炸弹的异常夹具和整手收官边界；该代码门不构成性能或强度证据。
6. 任何算法、预算或 checkpoint 结构修改，都先跑机械状态隔离 smoke，再用专门的全新诊断种子跑 10 区组严格性能探针；探针收益不得用于强度结论。
7. 只有探针整体及各运行段都通过固定 `500/750ms` 门，才预登记另一批全新 80 区组正式正常臂。
8. 正常臂必须同时通过 R0、完整性、强度和性能门。任一失败立即停止，不运行 fxe，不换种子挑结果。
9. 正常臂全部通过后，才按预登记运行同批 fxe，并用与主赛不重叠的种子完成当前源码连续赛、严格真人盲评和统一发布验证。

退出条件：所有硬门在同一冻结实现、同一正式种子清单和互绑回执上通过；否则 expert 保持默认。

### R3：现代 DMC / DanZero

依赖：R2 获得可复现的正向搜索基线，且 Python 环境前置完成。

1. 将 `guandan-env-v1` / DMC preflight 从格式脚手架加固为 fail-closed 门：严格拒绝非有限数、非法手牌计数、错误计数类型和伪摘要，并把 `training/*.py` 纳入语法与负例回归。
2. 保持外部数据和 R1A 真人对局候选批次 `trainingEligible=false`；外部数据先完成许可、规则版本、暗牌边界和按对局切分审计，真人数据先完成用户同意、规则重放、事件链/候选唯一性和按整场切分审计。
3. 实现独立 Python 规则环境与 JS↔Python 10 万条转换/动作差分，要求零差异。
4. 仅使用公平自对弈建立 PyTorch DMC；训练清单绑定数据摘要、种子、特征/动作 schema 和运行环境。
5. ONNX 导出与 Worker 加载必须逐级安全回退；模型仍需同牌、跨级、连续赛、性能和真人盲评全部通过才能晋级。

### R4：长期对手建模

依赖：不阻塞 R1；不得影响离线公平评测。

- 对公开画像增加分布漂移检测、冷启动/低置信自动降权和独立留出集。
- 个性化采样权重仅能使用公开行动，不能反推或读取暗牌。
- 用独立长期留出回执证明收益且不增加灾难性错误；否则保持 `observe` 或关闭。

## 四、0831–0902 证据台账

### 0901 Kimi K3 扩展评审复核

- 成立：固定下家五张内的弱调节不能覆盖任一方向报单，`preferredTypesForEnemyCount(2)` 还会软奖励领单；队友让牌分支前存在一手出完、紧急拦截和 P3 护牌提前返回，`partnerFinished=true` 时会跳过队友分支并进入普通接牌路径。
- 成立并已收口：此前满手修复的共享策略/教练阈值不一致、且 `strategy-core` 与 `ai.js` 重复扣分；本轮已改为独立 `reserveHighControlLead` 候选门，统一按出牌后剩余张数 `>8`，正式 expert/baseline 保持关闭。教练入口的重复事件也已移除，`premature_high_control` 现在只由共享策略事件写入一次。
- 修正：对家头游后的端局目标不是完全缺失，已有 `survival_preserve_control`、`placementControl` 和对应测试；应补复盘优先级反例，不应重写或分座位复制整套规则。`POLICY_FEATURE_KEYS` 当前为 18 项，不是评审所称 17 项。
- 证据限制：盲评路径经 `aiDecisionContext` 只向决策器传行动座位手牌与公开信息，相关严格门通过；但分析工具读取完整 `initialHands` 以逐座重建，原始导出删除后，15 副/1109 决策/55 次搜索/1 次改选、7 次送单和 3 次压队友均无法本轮独立重算，只能标为历史摘要。R2 检测是风险启发式，包含未转化和对手失误，不能把 7 次全部当同等严重 bug。
- 前序定向验证：`rules.test.js` 69/69、`ai.test.js` 363/363、`ai.hybrid.test.js` 81/81、`game.test.js` 70/70、`test_blind_eval_tools.mjs` 严格门通过；随后前序默认统一入口记录为 39/39，`-FullData` 记录为 45/45。本条为 0901 前序记录，不等于提交级 CI、镜像收益或发布证据；0902 本轮统一入口已运行至目标外缺失的 `tools/test_ai_ab_simulation.mjs` 并以 `MODULE_NOT_FOUND` 退出 1，RT-2 定向回归另见 R1A 记录。

### 0901 STRAT-2：长手高控制保留代码门

- `js/strategy-core.js` 新增唯一阈值 `RESERVED_CONTROL_LONG_HAND_REMAINING = 8` 与 `shouldReserveHighControlLead`；只有显式 `policyFeatures.reserveHighControlLead=true` 时，A/级牌三张或三带二才产生 `premature_high_control` 共享策略事件。
- `js/ai.js` 增加 `with-reserved-high-control-lead` 候选变体；正式 expert 与 baseline 均保持关闭。AI 的领牌排序不再为该特殊规则另加 `power×系数`，教练 `evaluator.js` 复用同一 helper、同一阈值和同一标签。
- 回归覆盖 11 张边界不触发、13 张三张 A、14 张三带二带对 A、级牌逢人配多解声明、两手收官豁免、默认关闭，以及教练只写入一次共享惩罚；与 STRAT-3/4 合并回归后的 `node js/ai.test.js` 为 **363/363**。没有运行新的正式镜像赛，故不形成收益/灾难率/晋级证据。

### 0901 STRAT-1：脱敏反例夹具与证据边界

- 新增 `tools/strategy-counterexamples.json`、`tools/strategy_counterexamples.mjs` 和 `tools/test_strategy_counterexamples.mjs`；夹具只保存行动座位手牌、公开历史、合法候选与预期不变量，不保存暗牌、未来牌或原始复盘导出。
- 夹具绑定 `guandan-rules-v1`、`js/ai.js`、`js/cards.js`、`js/evaluator.js`、`js/rules.js`、`js/strategy-core.js` 的逐文件 SHA-256 与聚合摘要；observation 顶层严格白名单并有换名暗牌负例；4 个 STRAT-4 机制场景的 schema、候选合法性、方向/状态不变量回归为 **24/24**。这只证明可复算代码门，不是镜像收益、灾难率或发布证据。

### 0901 STRAT-3：任一对手报单安全领牌代码门

- `strategy-core` 新增 `assessEnemyReportLead` / `filterEnemyReportLeadCandidates`，按所有未出完敌方座位的公开剩余张数识别上家、下家报单，不再只看固定下家；过滤器兼容紧凑顶层 `tags` 与 `{play,strategy}` 包装，保留原候选对象以避免解释/遥测字段丢失。
- `enemyReportLeadSafety` 与 `with-enemy-report-lead-safety` 独立默认关闭；只有完整合法候选中存在不拆炸弹、逢人配或王的安全非单，或已有 `public_lock_lead` 锁牌路线时，才过滤 J 及以下低单。没有替代动作时保留原行为，整手收官和跟牌分支不触发。
- AI 领牌、解释/咨询、教练共享策略和本地混合候选池复用同一过滤器；被阻断候选写入唯一 `enemy_report_lead_guard` 事件，混合层不允许旧式低单首选重新成为搜索锚点，避免“检测到报单”与最终选牌口径漂移。该规则未进入 expert 默认，也没有把历史复盘启发式命中数当作真实失误率。
- 回归覆盖上家/下家同时报单、仅低单与炸弹的无替代回退、共享事件唯一性、默认关闭、跟牌隔离、紧凑顶层 `tags`、`{play,strategy}`、混合咨询/搜索回退、资源差异安全锚点、教练备选和真人评价；本轮 `node js/ai.test.js` 为 **363/363**，`node js/ai.hybrid.test.js` 为 **81/81**。没有运行正式镜像赛，故不形成收益、灾难率、性能或晋级证据。

### 0901 STRAT-4：队友牌权硬优先级代码门

- `strategy-core` 新增 `assessPartnerTrickControl`：先以公开历史确认对家仍是本圈赢家，再确认活跃对手均已过牌或出完；缺少历史证据、对家不是本圈赢家或仍有可行动对手时 fail closed；对家已出完但仍持有本圈牌权时，按显式 `partnerFinished` 状态进入接风门。
- `partnerTrickControl` 与 `with-partner-trick-control` 独立默认关闭。AI、`evaluator` 教练评价和 hybrid 咨询在 P3、紧急拦截与普通接牌前统一优先接风并写入唯一共享约束；`partnerFinished=true` 不再静默进入普通接牌，整手出完为明确例外。
- `node tools/test_strategy_counterexamples.mjs`、`node js/ai.test.js`、`node js/ai.hybrid.test.js` 分别为 **24/24**、**363/363**、**81/81**；没有运行正式镜像赛，候选不进入 expert 默认。

### 0901 实时复盘与后续训练需求审计

- 已满足的数据基础：`js/game.js` 在每次出牌/过牌时向 `trickLog` 追加动作、圈号、座位、牌张/牌型、前后公开余牌数、评价与决策元数据；副末复盘还保存起止时间、名次、升级、完整初始/剩余手牌和汇总，`js/stats.js` 在浏览器保留最近 100 副并支持手工 JSON 导出。
- 0902 RT-3 已补齐实时公开访问：`lan_server.py` 提供默认关闭的 `POST /api/replay/events`、`GET /api/replay/events?afterSequence=...&limit=...` 和状态端点；写入受同源、JSON/大小/速率、公开字段/摘要/链校验，读取要求短期 capability token，支持 `matchId` cursor 与最多 5 秒短轮询。事件只落在安全的 `%LOCALAPPDATA%\GuandanTrainer\replays\`，按日/容量轮转、保留期清理、原子追加并 `fsync`；坏链、磁盘/文件故障 fail closed 且状态/UI 显示缺口。`setUpdateCallback` 仍只是页面重绘回调，`setAIDecisionObserver` 仍只服务评测/自对弈，二者都不是外部消费协议。
- 不可复用的危险捷径：`saveActiveMatch(serializeMatchState(state))` 会频繁保存完整 `hands` 与 `roundInitialHands`，虽然看似“实时”，但直接暴露它会让智能体看到四家暗牌并污染实时建议和后续公平训练，因此明确禁止把 active-match 快照作为复盘 API。
- 未满足的训练链：现有导出是 UI 复盘对象，不含逐手固定 schema、事件/实现摘要链、行动座位视角的公开 observation、完整合法候选、唯一 chosen、同意/来源和 `trainingEligible`；`replay_ai_audit.mjs` 是事后反事实分析器，不能把其意见自动当训练标签。
- 结论：需求当前为**部分满足**，已具备 RT-1/RT-2 的公开事件契约、真实动作观察器、本机待发队列、RT-3 的 opt-in 公开事件接口、RT-4 独立只读消费/annotation 和 RT-5 副末密封批次/离线转换；仍没有 RT-6 端到端生产验收，不能把本机公开流或未批准密封批次直接用于再训练。R1A/RT-6 仍是后续待办。

### 0902 RT-1 三层契约代码门（承接 0901）

- `js/replay-contracts.js` 新增 `guandan-live-public-event-v1`、`guandan-sealed-training-turn-v1`、`guandan-agent-annotation-v1` 的构造器与校验器；三类对象均绑定唯一 match/round/trick/turn、`eventId`、`sequence`、时间、规则版本、实现 SHA-256 和前序事件摘要。
- 公开事件只按白名单保留动作牌张、牌型、四家余牌数、贡还、引擎和脱敏决策元数据；密封 turn 才保留行动座位手牌、公开 observation、完整候选和唯一 chosen，并强制 `trainingEligible=false`；annotation 只保留 `eventId`、源事件摘要、模型/提示版本和独立分析内容。RT-4 消费者不复制源事件，也不写入牌局状态。
- `js/replay-contracts.test.js` 通过 **41/41**，并已接入 `tools/verify.ps1`；运行时事件观察器与本机待发队列已在 RT-2 完成，RT-3 又补齐默认关闭的 HTTP 公开采集、cursor/短轮询和短期读 token，RT-4 补齐 `tools/replay_consumer.mjs` 的断点消费和独立 annotation，RT-5 补齐副末密封批次和离线转换。R1A 仍是部分满足，不能宣称 RT-6 生产验收或训练准入已就绪。

### 0902 RT-2：真实动作提交边界与本机待发队列代码门

- `js/game.js` 新增独立 `setReplayEventObserver`，在 `applyPlay` / `applyPass` 完成状态写入后发出公开 `play`/`pass`，在真实收圈与 `endRound` 边界发出 `trick_end`/`round_end`；事件带稳定 `eventId`、全局单调 `sequence`、前序事件摘要和当前副的 `matchId`，公开牌不含实体牌 ID 或副本索引，公开决策元数据只保留受控 token，结束事件拒绝动作载荷，观察器异常被隔离，不改变出牌状态机。
- `js/ui.js` 将观察器事件送入 `js/replay-event-queue.js`。队列以 `guandan_replay_pending_v1` 有界持久化公开事件，按 match 分段排序，重发按 `eventId`/摘要幂等；漏序、冲突、队列溢出和持久化失败显式 fail closed，HTTP 业务拒绝、无身份或 `eventId`/摘要错配回执不会确认队首，刷新后仍锁存溢出或一般完整性缺口（即使旧记录漏写 `integrityGap` 标记也由链本身发现并锁存），无可用持久化存储时标记非 durable 并阻断提交；网络提交保持默认关闭，服务失败仅异步退避重试，不阻断牌局。完整事件链校验强制从 `sequence=0` 开始；实现摘要由真实动作边界与完整契约序列化依赖清单共同绑定。
- 本轮实证：`node js/replay-contracts.test.js` **41/41**、`node js/replay-event-queue.test.js` **39/39**、`node js/replay-observer.test.js` **12/12**；`node js/integration.test.js` 完整一副回归通过且构造失败为 0，覆盖真人与三个 AI 的真实动作事件、贡还、接风、双上提前结束、打 A 终局及公开暗牌边界。
- 明确边界：当前仍无 `lan_server.py` 的 POST/GET、cursor/token、第二消费者、HTTP 真实端到端、密封训练转换或 UI 配置；本轮完整 `verify.ps1` 在目标外既有改动删除 `tools/test_ai_ab_simulation.mjs` 后于该步骤以 `MODULE_NOT_FOUND` 退出 1，未运行 `-FullData`、正式大规模镜像赛、远端 CI 或发布晋级验证，expert 默认与发布状态不变。
- 0902 收尾独立只读审查确认 RT-2 代码范围 PASS、无 P0/P2；RT-3/RT-4 另有本轮代码门和回归记录。整体交付仍不签署发布 PASS，RT-5～6 未完成状态保持不变。

### 0902 RT-3：opt-in 本机公开采集与游标代码门

- `lan_server.py` 新增默认关闭的 `ReplayEventStore` 与 `POST /api/replay/events`、`GET /api/replay/events`、`GET /api/replay/status`；仅服务现有 `127.0.0.1` 回环监听。`start-lan.ps1 -EnableReplayCollector` 才启用采集，并生成最多 60 分钟有效的短期 capability token；读取端点用 `X-Guandan-Replay-Capability` 校验，未授权返回 401。
- 写入端点只接受同源 JSON，限制 64 KiB 请求体和每分钟 240 次请求；Python 侧独立复算公开 schema、严格字段、暗牌禁用字段、事件摘要和 sequence/前序摘要链，重复 `eventId`+摘要幂等，冲突/缺口拒绝。事件按日和容量写入 `%LOCALAPPDATA%\GuandanTrainer\replays\`，目录拒绝项目树/WPSDrive，文件按 0600 原子追加并 `fsync`，过期/超限清理失败、损坏或磁盘满显式返回 `gap`/故障。
- GET 支持 `afterSequence`、`matchId`、`limit` 和最多 5 秒短轮询；`nextSequence`/`hasMore` 可供第二消费者续读。`/api/replay/status` 和 UI 状态胶囊只显示启用、待发和缺口，不暴露 token 或暗牌。
- 当前回归：`python -X utf8 lan_server.test.py` **39/39**，`node js/ui.static.test.js` **45/45**，`node tools/test_replay_consumer.mjs` **20/20**，统一 `pwsh -NoProfile -ExecutionPolicy Bypass -File .\tools\verify.ps1` **43 checks**。这只是本机公开流/只读消费者代码门；RT-5 密封训练、RT-6 真实 HTTP 对局和端到端验收仍未完成，expert 默认、发布/晋级状态不变。
- 独立复核记录：首轮只读复核在并发中间态定位了 `hand.meta` 嵌套实体字段、容量清理静默截断、页面/启动器未真正启用采集，以及跨语言数字摘要、短写回滚和游标身份等问题；当前树已修复并由 39/39 网关回归、45/45 UI 静态回归和 42 项统一入口复验。修复后再次复核在交付前超时，`codexhost`/可验证 Sol 入口不可用，故不宣称独立 Sol PASS；这不改变 expert 默认、发布或晋级状态。

### 0902 RT-4：只读复盘消费者与独立 annotation 代码门

- 新增 `tools/replay_consumer.mjs`：CLI 只访问 `GET /api/replay/events`，严格限制 `http://localhost`/`127.0.0.1`，token 只从显式参数或 `GUANDAN_REPLAY_CAPABILITY` 读取且不打印；支持一次消费与显式 `--follow` 短轮询，不调用 POST、不触碰当前牌局或源事件。
- 每页按已确认的 `afterSequence` 读取；首事件必须从 `sequence=0` 开始，后续必须匹配同一 `matchId`、连续 sequence 和前序 `eventSha256`，并由 `validateLivePublicEvent` 复核公开字段与内容摘要。HTTP/JSON/链缺口、服务端 gap、网络故障或本地存储损坏均 fail closed；网络失败不推进已确认 cursor。
- 游标以原子 JSON 写入 `%LOCALAPPDATA%\GuandanTrainer\replay-consumer\cursor.json`；annotation 以独立 0600 NDJSON 追加至 `annotations.ndjson`，短写回滚，源事件已处理但 cursor 尚未提交时按确定性 annotation ID 幂等恢复。annotation 通过 `createAgentAnnotation`/`validateAgentAnnotation`，仅记录模型、提示版本、创建时间和源事件摘要，内容为手/圈/副聚合复盘，不含源事件副本、暗牌、收益、`trainingEligible` 或自动训练标签。
- `node tools/test_replay_consumer.mjs` **20/20**。本代码门证明本机适配器的读/写隔离与恢复逻辑，不证明真实双消费者 HTTP 对局、RT-6 生产验收；整体发布、晋级和 expert 默认状态不变。

### 0902 RT-5：副末密封训练候选与离线转换代码门

- `js/game.js` 在 `applyPlay` / `applyPass` 改变手牌前捕获行动座位手牌、公开 observation、`generateLegalPlays` 完整候选和唯一 chosen；公开观察器仍然只接收 `guandan-live-public-event-v1`。过牌写入显式 `pass` 候选；非法实际动作 fail closed，不写入密封 turn。
- 副末 `finalizeSealedTrainingBatch` 要求密封 turn 数量与公开 play/pass 一致，并按 `finishOrder` 重算双上/头三/头末升级和相反团队收益。`createSealedTrainingBatch` 强制 `trainingEligible=false`，拒绝 `reward`/`outcome`/`trainingLabel` 反事实字段。
- `serializeMatchState` 剥离 `sealedTrainingTurns` / `batch` / `history` 等密封字段，因此进行中存档和刷新恢复都看不到密封内容。转换器默认写 `%LOCALAPPDATA%\GuandanTrainer\sealed-training\`，拒绝项目目录、WPSDrive、公开 `replays/` 和 `replay-consumer/`。
- `convertSealedTrainingBatches` 逐条重放规则合法性、牌张守恒、已出完座位、名次前缀、同一座位不得连续行动、候选完整性/归属和唯一 chosen：规则生成集必须被保存，未选择候选不得引入行动手牌之外的牌张，失败时切分数组保持为空。去重后按 `matchId` 的 SHA-256 分桶切分 80/10/10 的 train/validation/held-out。任一批次失败则整体 fail closed 且不写切分文件。
- 评测依赖闭包因 `game.js` 新增 `js/sealed-training.js` 从 21 增至 22；`tools/test_ai_ab_simulation.mjs` 已把该文件纳入源码指纹变异拒绝。这会让旧 checkpoint 的 implementation SHA 失效，符合 EVID-3，不构成新的发布证据。
- `node js/sealed-training.test.js` **32/32**，`node tools/test_sealed_training_converter.mjs` **8/8**，观察器 **14/14**，完整一副集成回归通过。独立只读审查：P0 为 0；首轮 P1 为转换器对未选择外来候选校验过松，当前树已收紧候选归属并补负例。边界：密封批次只存在于当前对局内存，刷新即丢失；座位轮转只禁止同一座位连续行动。expert 默认、发布和晋级状态不变。

### 0902 RT-6：回环 HTTP 端到端安全与完整性代码门

- `tools/test_replay_e2e.mjs` 启动临时回环采集器，把一副双上收束对局的公开事件经真实 HTTP POST 写入；两个 `replay_consumer.mjs` 从中途 cursor 续读，公开事件无 `hands`/实体 ID；同一副密封批次可转换且训练器拒绝 `trainingEligible=false`。capability token 只走环境变量，状态 JSON 不含 token。
- `lan_server.test.py` 增补暗牌注入、跨 origin POST、超大请求 413、重复幂等、过期 token、磁盘满和保留期删链；保留期/容量删除已提交事件会锁存 gap，避免空目录被当成新链（0902 复审后改为链楼层台账：有意清理登记楼层继续服务，未登记的外部删除才锁存）。Host 允许 `127.0.0.1:<回环端口>`，Origin 仍须为空或生产回环 origin。
- UI 采集控制提供暂停/恢复/清空待发；`replaySubmitPaused` 不会被 5 秒状态刷新覆盖。启用仍须 `start-lan.ps1 -EnableReplayCollector`。状态展示最后序号、保留期和智能体是否连接。
- `python -X utf8 lan_server.test.py` **47/47**，队列 **41/41**，UI 静态 **47/47**，e2e 通过，统一入口 **46 checks**。独立只读审查：本地代码门 PASS（有残余风险），P0/P1 为 0。边界：合成收束对局不是浏览器真人长局；清空只清本机待发；密封仍无刷新持久化；不是训练准入。expert 默认、发布和晋级状态不变。
- 0902 复审追加：第二轮三线程审查发现 1 个 P1（capability token 闲置过期后无续期路径，长局必断链）与 12 个 P2（明细见 `todo.md`「RT 审查残余风险清单」）。P1 已在当前树修复：`authorize()` 成功授权滑动续期、新增 `capability_expired` 错误码、`start-lan.ps1 -EnableReplayCollector` 在 token 过期时安全重启续签、消费者识别过期并提示重新签发；回归为 `lan_server.test.py` **50/50**、`tools/test_replay_consumer.mjs` **22/22**、e2e 通过。12 个 P2 均未修复，作为已记录残余风险保留。本轮 RT-3～RT-6、P1 修复与文档已随提交 `1331550` 入库（统一入口 46 checks），尚未推送远端；边界与停止线不变。

### 0902 STRAT-5 与 RT 残余代码门（未提交）

- STRAT-5：`js/strategy-core.js` 的 `filterEligibleStrategyActions` 为唯一 eligible-action 层；expert 领出、`getAIConsultation`、混合咨询、`evaluateInformationSetCandidates` 和本家 ISMCTS 内节点共用。feature 关闭返回同一数组引用；STRAT-3 阻断后不再还原原集合；跟牌缺 pass 时注入过牌，领出空集不合成 pass。PIMC / 成对根 PIMC / ISMCTS v2 / ISMCTS v3 注入低单或抢队友牌均不能成为最终动作。`chooseRolloutPlay` 未改。`enemyReportLeadSafety` / `partnerTrickControl` 仍默认关闭。
- RT 残余：Infinity/NaN 与嵌套深度 12 / `RecursionError` 映射为干净 400/503；GET 独立 240/60s 限流；`--once` 完整分页；annotation 最后一行半写可截断恢复；`upgradeCode` 必须由名次派生；跨座位 `cardIdentity` 唯一；恢复对局 seq>0 且队列无本局痕迹时停采并提示新开一局。`game.js` 未改，采集器默认关闭，`trainingEligible=false`。
- 本轮实证：`node js/ai.test.js` **369/369**，`node js/ai.hybrid.test.js` **105/105**，夹具 **24/24**，`lan_server.test.py` **58/58**，消费者 **25/25**，密封 **36/36**，转换器 **10/10**，队列 **50/50**，UI 静态 **51/51**；当时 `verify.ps1` 退出 0、**46 checks**。独立 Sol 最终审查 PASS（有残余风险），P0/P1 为 0。残余：append 全量扫描（0903 已由进程内事件索引消除，见下）、内节点 STRAT-4 仍读根 `publicHistory`（0903 已修复，见下）、rollout 估值可走被禁线（0903 评估后不做，理由记入 todo.md）。未运行 `-FullData`、远端 CI 或正式种子。expert 默认与发布状态不变。

### 0902 STRAT-6 正式消融与 REL 脚本化封版（已随提交 `c7e4c9b` 入库）

- 预登记：`tools/strat6-seed-registry.json`，对照 expert、`opponentModelMode=off`、全 13 级 level-blocks；正式 `20268111–20268150`（40 区组，520 对），smoke `20268101–02` 已消耗。实现 SHA `94cccaddac6e29c08b3d69ff8f35aac716bb881909cb173c8400e1010dd5d8ed`，与评测时 22 文件工作树闭包一致；0903 审计发现 `c7e4c9b` blob 因 `core.autocrlf` 在 `js/ai-hybrid.js`/`js/ai.ab.simulation.js`/`js/ai.ab.telemetry.js` 上 CRLF→LF 归一（blob 聚合 `8c6b1224…`），字节级提交冻结待修复。
- 顺序正式臂 exit 0，合计 8641.61s。报告 SHA-256：strat2 `8bb14602b5683d6d8637210d44907565a6ecaebce438ccaa452bf06e812e65eb`，strat3 `9e8dbd44dff06a3042ccb3cd70300a797efd5cafda5de6ee2f8ecb5817ef4249`，strat4 `e3ef13f711622a2212786688cab2ae5b0ddaf2b4ddfa3d5529e2024ebf9c5c67`。
- STRAT-2 `with-reserved-high-control-lead`：1040/520/40，0 失败，CI `[0.047,0.188]`，双上 255/220，数字门通过。级牌 2/6/8/10 为负；先手下家/上家效用为负。0903 晋级审查：数字属实、分层负项属噪声不阻断（翻转时须披露），字节冻结阻断已修复；按用户授权翻转后 `reserveHighControlLead: true` 进入正式大师默认（baseline 保持 false）。
- STRAT-3 `with-enemy-report-lead-safety`：CI `[-0.015,0.041]`，双上 240/243，CI 与灾难失败，保持关闭。
- STRAT-4 `with-partner-trick-control`：CI `[0,0]`、双上 237/237，40 个区组均值全 0，本种子集与 expert 完全等价，下界未严格 >0，保持关闭。规则已接入 expert 决策路径但触发条件苛刻，零分歧说明本消融对该规则无证据力，而非证明无用。
- 三臂 `searchTriggered.decisionTurns` 均为 0（expert 对 expert）；搜索性能门不适用。贡还层不可识别。summarizer 现读取正式报告 `performance.decisionLatencyByPolicy`。
- REL：脚本化三副 `ok=true`（升级 finishOrder `0,2,3,1`、贡还出现、打 A 过 A）；`data/expert-release-acceptance.json` SHA-256 `5c32d72a8bf1f405e231c9ba522f09be79542851dc06506e8dd7182610a7b833`。笔记本/手机卡顿未测。README 已收敛；复盘标注 ISMCTS v3/成对 sweep 且不进选择器。
- `node tools/test_strat6_ablation.mjs` **15/15**，夹具 **24/24**，`pwsh -NoProfile -ExecutionPolicy Bypass -File .\tools\verify.ps1` 退出 0、**48 checks**。晋级咨询 KEEP_CLOSED；终审 PASS（有修改）。未运行 `-FullData`、远端 CI、ReleaseEvidence。已随提交 `c7e4c9b` 入库（0903 复核），未推送、远端 CI 未跑。

### 0903 夜间自主执行：审计、字节冻结、STRAT-2 晋级与 RT 残余清零（ZCode 会话）

- **独立审计**：对 0902 Grok 夜间交付（`c7e4c9b`）逐声明复核，全部属实；发现并纠正审计方自身的两处误判（分层"全零"系 grep 行数误判；"22/22 绑定确认"系工作树字节级而非提交 blob 级——`core.autocrlf` 在 3 个闭包文件上 CRLF→LF 归一，blob 聚合 `8c6b1224…`≠`94cccadd…`）。
- **字节冻结修复（`1d66d75`）**：新增 `.gitattributes` closure-eol 宏（22 个评测闭包文件 `-text` + `whitespace=cr-at-eol`），renormalize 后提交 blob 22/22 逐文件与聚合 `94cccadd…` 完整复现（HEAD blob 复算 + 审查三方互证）。
- **STRAT-2 晋级翻转（`d552238`）**：按 0903 只读晋级审查条件（数字门属实、searchTriggered 门不适用、分层负项不阻断、字节冻结修复）与用户授权，`reserveHighControlLead` 进入正式大师默认（baseline 保持 false）；夹具指纹刷新（`2d37d72c…`）；旧"默认关闭"断言更新为晋级状态并加严守住 STRAT-3/4 与 controlRiskV2 关闭。残余风险（级牌 2/6/8/10、级牌 8 -0.125、下家 -0.154/上家 -0.385、队伍不对称 team0 -0.042/team1 +0.273）与"不得宣称 promoted/releaseEvidenceReady"已披露。翻转后发布证据须与新提交重新互绑。
- **RT 残余三项清零/收窄**：#13 残余（`31a5969`：clearPending 登记持久化 brokenMatches 断链标记，续写被干净停采）；#9 残余（`2cadada`：撕裂末行 JSON 完整但校验失败可截断恢复，已终止末行仍 fail closed）；#5 收窄（`64d66f6`：append/read/status 每请求全量重扫由进程内事件索引消除，外部改动经分片字节比对失效重建，等长篡改检测窗口收窄已披露）。
- **STRAT-5 P2-a 修复 + P2-b 决策（`7b09f8f`）**：ISMCTS 本家内节点 filterCtx 隔离根 publicHistory，让牌判定封闭于模拟世界（三组新断言，hybrid 108/108）；P2-b（rollout 套用 STRAT-3）评估后不做（破坏 ISMCTS v2 冻结基线、热路径开销、先验二次计入），记为已评估残余。
- **验证与审查**：每项任务独立只读审查（均为 pass-with-notes、零阻断，notes 已采纳/披露）；`lan_server.test.py` **61/61**、队列 **58/58**、消费者 **27/27**、hybrid **108/108**、ai **369/369**（一条环境性墙钟断言在机器热节流期间失败，A/B stash 计时证明与改动无关，冷却后复跑见台账）、统一入口 **49 checks**。注意：`ai.test.js` 的 ai.test 行数因 STRAT-2 断言改写仍为 369。
- **晨间收尾（用户在场）**：冷却复跑统一验证 **49 checks** 退出 0（环境性墙钟断言恢复 `ai.test.js` **369/369**）；经授权推送 `54c2d04..daf6947` 并通过远端 verify CI run [`33703113243`](https://github.com/floating-life/new-guandan/actions/runs/33703113243)（3m40s）；**REL-2 真人手打/手机视口卡顿实测完成**（`data/expert-release-interaction-measured.json`：笔记本 1280×720 与手机视口 390×844 各约 4 分钟实测，Chromium ~89 FPS、P95 帧间隙 12ms、共 4 次 0.3-0.9s 停顿、内存峰值 48MB、localStorage 运行时抽查无 API Key；边界：视口模拟非真机、普通难度），todo.md REL-2 条目更新。
- **边界**：0903 晨间经用户授权推送并通过远端 verify CI run [`33703113243`](https://github.com/floating-life/new-guandan/actions/runs/33703113243)；未消费新正式种子、未启动正式长跑/训练；不宣称 promoted 模型或 releaseEvidenceReady。`draft-strat6-rel-followup.md` 执行日志保持未跟踪。

### 自动验证与价值模型

- 0831 的 31 项统一验证只作历史基线。0901 前序工作树已清除 5 个未合并索引路径和 7 个 EOF 空白行；`git ls-files -u`、工作区 `git diff --check` 与 staged `git diff --cached --check` 均为零，统一入口也已显式拒绝未合并索引。
- 0901 前序的 34/34、30/30 等数字均仅作历史记录；HEAD `6337e8b` 之上 STRAT-5/RT 残余后统一入口曾为 **46 checks**；计入 STRAT-6/REL 工具测试后重新执行 `pwsh -NoProfile -ExecutionPolicy Bypass -File .\tools\verify.ps1`，明确输出 **48 checks**。其中 `ai.test.js` **369/369**、`ai.hybrid.test.js` **105/105**、夹具 **24/24**、`lan_server.test.py` **58/58**、消费者 **25/25**、密封 **36/36**、转换器 **10/10**、队列 **50/50**、UI 静态 **51/51**、观察器 **14/14**、`test_strat6_ablation.mjs` **15/15**。验证器仍只证明当前代码回归，不构成发布或晋级证据。
- 当前索引没有冲突；STRAT-5/RT 残余与 STRAT-6/REL 工作已随提交 `c7e4c9b` 入库（0903 独立审计复核），未推送、远端 CI 未跑。`tools/extracted/` 不存在且已被 `.gitignore` 覆盖；严格价值模型发布校验仍按预期返回 1，不能把定向回归解释为可提交或可发布。
- 当前价值模型 `data/value-model-20260901-experimental.json` 有效，但没有 `promoted` 模型。
- 实际严格发布校验当前返回 1：主 A/B 仍绑定旧文件摘要，且历史 95% CI 为 `[-0.008,+0.029]`。EVID-1 已令退出码只随 `promotion.promoted=true` 成功；真实失败仍不能被“当前恰好失败”掩盖为门禁闭环。

### 0901 EVID-8：M2 统一发布验证器（本地代码门已关闭）

- `tools/validate_m2_release.mjs` 是只读统一门：先复算当前源码依赖闭包和机器/Node provenance，再逐对象检查正常臂完整 80 区组 × 13 级 × 双腿、v3 checkpoint integrity、唯一 seed/level/team 遥测、search-triggered 性能及每运行段守恒，最后互绑 8 场连续赛与盲评 manifest/selected scenario 载荷/全题 catastrophic review。
- `tools/test_validate_m2_release.mjs` 用自包含合成工件证明正例可通过；负例覆盖模型状态、checkpoint schema/utility/order/double-up/决策遥测、raw 负延迟/搜索字段矛盾、性能 aggregate/漏段、连续赛区组/seed、盲评 allocation/gate/catastrophic、场景 implementation hash、binding 篡改和跨零 CI，均返回非 0。
- 0901 继续复审补上主报告 `performance.environmentTelemetry` 根字段的 fail-closed 检查；汇总器和 M2 均同时拒绝根字段及 `byRunSegment[*]` 诊断遥测，两个 root-level 负例回归均通过。
- `tools/test_environment_telemetry.mjs` 现固定覆盖 Windows junction / 目录 symlink 真实别名与核心 checkpoint 的冲突拒绝（受限非 Windows 平台显式跳过），使路径保护不只依赖一次性手工实测。
- `js/game.js` 对普通 expert 回合写入显式 `searchAttempted=false/searchTriggered=false/fallbackKind=none`，对本地超时/错误保留 fallback 类型；异常 hybrid 元数据保持未知。`tools/test_ai_ab_simulation.mjs` 与 `js/game.test.js` 均通过。
- `tools/verify.ps1 -ReleaseEvidence` 现会传入 `--blind-scenarios` 和 `--blind-catastrophic`；EVID-9b 冻结提交 `585f099` 的远端 CI 已形成，但当前工作树 STRAT 改动尚未冻结，且没有真实完整 M2 工件，因此仍不宣称 `releaseEvidenceReady` 或 `promotion.promoted`。

### 0901 PERF-1：statefix 历史性能分段诊断（本地诊断已完成）

- `tools/analyze_statefix_performance.mjs` 只接受 `guandan-ai-ab-checkpoint-v2`，以 `legacyHybridAppliedProxy` 明确重建 search-triggered 代理；逐条校验签名、完整 block×level×team 覆盖、唯一坐标、pair↔game/order、零失败，并把报告的总体计数、均值/P95/P99、最大值、回退、资源计数与 coverage 全量绑定到 checkpoint 复算。
- `tools/test_analyze_statefix_performance.mjs` 覆盖完整/缺失/重复/篡改、百分位、报告 coverage 伪造和外部分段 sidecar 绑定；`verify.ps1` 已接入该合成回归。最新定向测试与语法检查均退出 0。
- 对 `data/eval-20260830-ismcts-v3-statefix-normal-ab80.{json,checkpoint.json}` 的本机复算：6677 条、coverage `6677/6677`、总体 P95/P99 `619/1022ms`；区组 1–60 为 `5058`、`421.8/550ms`，61–80 为 `1619`、`942/1481ms`。v2 不含内生 run/resume、RSS/heap/GC/I/O 或 CPU/电源指标，故 `formalGateEligible=false`，性能门仍失败，不能据此继续同批 fxe。
- 诊断输出：`data/performance-diagnostic-20260901-statefix.json`，SHA-256 `21cb4c0f12c82ae4afeb129d5865b4ee4dc702d9e74d12a76786f36262eec377`；该文件仅为本机历史诊断，不是发布回执。

### 0901 PERF-2 / ALGO-1 / ALGO-2：环境遥测接线、v3 事务回滚与 rollout 首出保护（代码门通过）

- `js/ai.ab.environment-telemetry.js` 提供显式 opt-in 的本地诊断采集：RSS/heap、进程 CPU、系统 CPU 累计时序/频率、GC、powercfg、电源状态、外部负载和 checkpoint 分阶段计时；Windows `os.loadavg()` 的无效值保持 unavailable，并明确以系统 CPU 时序/频率作代理。artifact 的 status、peaks、系统 CPU/外部负载摘要、checkpoint 序号与 GC 基本计数由样本/内容复算，矛盾或丢样只会 partial/unavailable。
- `js/ai.ab.simulation.js` 仅在 `--environment-telemetry` 下启用 collector，并以独立、原子 sidecar 保存；sidecar 不得覆盖 checkpoint、`.last-valid`、report 或 raw telemetry，损坏/缺段不会阻断核心 checkpoint resume。正式性能汇总器与 `validate_m2_release.mjs` 均拒绝携带 diagnostic-only `environmentTelemetry` 的输入，避免诊断 observer/I/O 被静默当作正式门证据。
- `tools/test_environment_telemetry.mjs` 覆盖快照、系统 CPU 代理、GC、路径冲突、artifact 篡改、checkpoint 计时唯一递增、fresh→中断→resume、损坏/缺段 sidecar；`node tools/test_environment_telemetry.mjs`、`node tools/test_ai_ab_simulation.mjs`、性能 provenance 和 M2 正负例于 0901 通过。
- v3 失败 sweep 在整棵开放环树快照上事务化；`js/ai.hybrid.test.js` 新增“成功深树→后置候选 rollout 注入失败→后续成功 sweep”回归，并证明深层 availability/子节点/visits/reward 等字段逐字段恢复。`node js/ai.hybrid.test.js` 当前 `81 passed, 0 failed`。
- ALGO-2 将 rollout 首出与跟牌分支分开：`lastHand=null` 时优先从合法普通候选出牌；若未来生成器只返回非收官炸弹，按确定性结构成本回退合法候选并标记 `lead_no_ordinary_fallback`，避免把首出误转为 `pass` 触发 `rollout_action_invalid`；整手炸弹收官保持正常 finishing 语义，v2/v3、PIMC/paired 和最终混合遥测均保留诊断对象。`js/ai.hybrid.test.js` 的正常首出、7/9 张只返回炸弹异常、实体牌合法性/乱序确定性、finishing、跟牌和搜索传播回归通过，前序定向结果 `81 passed, 0 failed`；0902 本轮统一入口在目标外缺失 `tools/test_ai_ab_simulation.mjs` 处退出 1，不能记为全仓通过。
- 以上均为本地代码/短 smoke 证据，不形成冻结提交、远端 CI、正式性能或晋级证据；真实大 checkpoint fresh/resume 诊断仍是 PERF-2b 待授权事项，expert 默认不变。

### 0901 ALGO-2：rollout 首出不变量（代码门通过）

- `chooseRolloutPlay` 在非空领出手牌上由规则本身的单张合法性保证正常路径总有出牌；新增回归覆盖代表性 9 张领出，禁止返回空动作。
- 若未来 `generateLegalPlays` 异常返回空集，模拟器从本家实体牌独立重新解析单张，按结构成本、牌力和稳定签名选择最便宜合法着法，并记录 `leadFallbackUsed`。若连该安全兜底也无法构造，两个 rollout 路径都以 `rollout_lead_missing_legal_play` 明确失败，不会把无领出误写成 pass 或静默保留不完整 sweep。
- `node js/ai.hybrid.test.js` 为 **74 passed, 0 failed**；随后默认 `tools/verify.ps1` 为 **38 checks**。该稳健性修复不改变搜索预算、性能门、收益、晋级或 expert 默认结论。

### 0901 TEL-1：v3 预算遥测语义闭包（代码门通过）

- `iterationBudget` 继续仅作为调用方输入的 rollout 总预算，保持现有配置、checkpoint 签名和历史 v2/PIMC 的真实 iteration 输入兼容。v3 搜索结果与决策遥测不再输出含义错误的 `iterationBudget`，改为同时报告 `rolloutBudget`、`sweepBudget=floor(rolloutBudget/candidateCount)` 和实际 `pairedSweeps`；`iterations` 仍是实际完成 rollout 数。
- 每手 `decisionTelemetry`、raw telemetry、force-expert 分析器和 M2 发布验证器均把上述三个计数纳入严格字段与非负整数校验，原始记录必须和 checkpoint 唯一坐标逐项一致，不能在 report/raw/checkpoint 间混用旧口径。
- `js/ai.hybrid.test.js` 覆盖 2～6 个候选并固定 `iterations == pairedSweeps × candidateCount`、v3 输出没有歧义 `iterationBudget`；结果 **80 passed, 0 failed**。`js/ai.test.js` **328 passed**，A/B checkpoint、fxe 与 M2 定向回归也通过。该改动只消除遥测歧义，不降低节点/墙钟预算，不改变性能门、收益或 expert 默认。

### 0901 ALGO-3：炸弹分支覆盖基线（尚未改变默认排序）

- 新增只读 `inspectOpenLoopBombCoverage`，直接对同一模拟状态比较当前 `branchLimit` 下的动作集和“保护专家首选/pass、以最小炸弹替换最后一个普通分支”的诊断动作集；它不接入正式 tree search、不改变节点预算，也不能作为强度或性能证据。
- 三个固定牌面回归表明：紧急可接与无目的领炸都存在合法炸弹但默认有限分支未覆盖的情况，诊断预留槽恢复一条最小炸弹；整手收官炸弹已经是专家首选，不需要也不应重复保留。`node js/ai.hybrid.test.js` 为 **83 passed, 0 failed**。
- 这只完成动作覆盖的可复算基线。灾难率、收益、节点数、P95/P99 尚未以新预登记种子测量，故默认排序保持不变，ALGO-3 继续开放。

### 0901 外部评审独立复核

| 外评项 | 独立判定 | 当前处理 |
|---|---|---|
| Chrome 存储导出泄漏 | **确认，提交前安全阻断。** `tools/extracted/018895.log_018895.log_logrecord.bin` 为 10,332,172 字节，SHA-256 `6cb58714b9c33cc40dc56a8eb033560c9664d966845945f0202254d410f0312d`；同一批字节同时命中本地掼蛋键、`console.volcengine.com` 和 UTF-16 令牌类标记。抽取器在整批记录只要出现 `guandan` 时就原样写出，目录未被 ignore，且仓库位于 WPSDrive。未输出或记录任何令牌值。 | 先由用户撤销/轮换可能受影响的会话并处理云同步副本；随后删除/隔离原始导出、加入 ignore，并把抽取器改为 origin+key 最小化白名单。完成前不提交/推送。 |
| ISMCTS 炸弹首出非法过牌 | **外评给出的现存 bug 不成立。** 首出 `generateFreePlays` 对每张实体牌必生成单张，`parseHandVariants` 对任意单牌都生成 `single`，所以有效的非空首出手牌不可能“合法着法只剩炸弹”。代表性 9 张手牌探针得到 17 个合法着法，其中 14 个非炸弹、3 个炸弹类；未复现 `rollout_action_invalid`。 | 不作为发布阻断；补“首出永不 pass”的防御性断言/回归仍有价值，避免未来规则生成器变化破坏该不变量。 |
| UI 暴露 `dmc-v1` | **确认，产品诚实性阻断。** 设置与复盘会保存/展示 DMC，运行时白名单不含 `dmc-v1`，实际走 expert；当前也没有 Python 规则适配器、ONNX 或 Worker 模型加载路径。 | 在可加载模型真正存在且晋级前移除选择项，并迁移已有 `dmc-v1` 设置为 expert。 |
| 内节点炸弹分支靠后 | **部分确认，属于实验搜索覆盖建议。** 默认 `branchLimit=5`，专家动作/pass 先占槽，炸弹结构成本额外 +32，确有可能被普通动作挤出；但这不构成规则非法，且专家安全筛选本就有意抑制无目的领炸。 | 先补动作覆盖率/灾难牌面回归，再比较“预留最小炸弹槽”对强度、节点数和尾延迟的影响，不直接改排序。 |
| v3 `iterationBudget` 字段 | **确认。** 输入 `iterationBudget` 是 rollout 总预算，v3 返回字段却写成 `floor(rolloutBudget / candidateCount)` 的 sweep 配额；`pairedSweeps` 本身仍可审计，所以不直接造成门禁假绿，但跨引擎遥测失真。 | 同时记录 `rolloutBudget`、`sweepBudget`、`pairedSweeps`，保留旧字段时明确兼容语义并补候选数变化回归。 |
| `verify.ps1 -FullData` 契约 | **已修正。** 当前 `-FullData` 与 ReleaseEvidence 的必需输入分支已分开；缺少连续报告的自包含回归通过，0901 前序工作树的 FullData 入口为 45/45（历史记录）。 | 仍需在 STRAT/RT-2 最终冻结提交上由 CI 重跑；本轮只重新执行默认入口，因目标外缺失 `tools/test_ai_ab_simulation.mjs` 退出 1，未形成当前全仓通过证据。 |
| `ai.js` 历史 Probe 注释 | **确认，低优先级整理项。** | 把运行历史移入证据台账，源码只保留预算依据和稳定约束。 |
| 复盘未单列 `ismcts-v3` | **确认，低优先级 UI 问题。** v3 目前显示为泛化“混合搜索”。 | 在 v3 仍离线时先补正确标签；不借此把 v3 暴露为可选引擎。 |
| “三个模式”列出四项 | **确认，nit。** | 改为“四个”；与其他文档整理一起完成。 |

外评总体方向“expert 仍是唯一诚实默认、搜索/DMC 未晋级”成立，但“炸弹首出导致整次 paired sweep 丢弃”不能作为已证实 bug。当前真正的第一优先级是浏览器导出安全处置，其次是移除 DMC 假选项、修复验证/遥测契约；分支排序优化必须经过性能与强度证据，不能凭静态偏好直接改。

### EVID-4：checkpoint 原子与可恢复（本地代码门已关闭）

- 当前工作树的 A/B checkpoint 为 `guandan-ai-ab-checkpoint-v3`，包含 `checkpointIntegrity:{schema:'sha256-v1',sha256}` 与运行 provenance；只有签名、内容摘要、`complete/nextBlockIndex`、精确 seed×level×team 覆盖、pair↔game 和 failure 派生关系同时成立才可 resume。
- 保存顺序为同目录唯一临时文件 → `fsync` → 重读校验 → `rename` 替换；绝不以“先删除目标”作为 Windows 回退。每次替换前把已验证 primary 原字节经相同流程写入 `.last-valid`；主坏时只从同配置有效备份恢复，主备均坏拒绝。
- `node tools/test_ai_ab_simulation.mjs` 的前序 0901 运行曾退出 0；0831 的 `verify.ps1` 31 项记录仅作历史基线。回归覆盖半写残留、主坏/主备双坏、可解析篡改、重复/错配对象、数组重排和 Windows `FileShare.None` 写锁；0901 前序工作树的默认 39/39、`-FullData` 45/45 均为历史记录，0902 本轮统一入口因目标外既有改动删除该测试文件而在调用步骤以 `MODULE_NOT_FOUND` 退出 1。
- 边界：未实现多个 writer 共用同一路径的互斥；SHA-256 是事故/篡改检测而非带密钥认证；不对断电时目录元数据绝对持久作承诺。**该子门关闭不代表 R0、性能、强度或发布门通过，expert 默认不变。**

### EVID-5：性能回执 provenance（本地代码门已关闭）

- `guandan-ai-ab-report-v1` 与 v3 checkpoint 将 `evaluationId`、评测机环境摘要、PID/PPID、fresh/resume 运行段链和输入 checkpoint 摘要写入并绑定；每局、镜像对和分段性能均有 `runSegmentId`。resume 必须与当前机器、Node/V8 环境完全一致。
- `summarize_ai_performance_baseline.mjs` 仅在本机逐文件复算当前评测依赖闭包和聚合 SHA 后，才可能生成正式回执；并固定要求 80 区组、精确 `[2..14]`、`opponentModelMode=off`、候选与当前解析后的 `searchMode` 一致。每个运行段分别受 ≥100 触发、≥99% 覆盖、P95≤500ms、P99≤750ms、回退<0.5% 的固定门约束，五项 `searchTriggered` 原始计数还必须与总体严格守恒。
- 回归拒绝伪造 64 字符摘要、同步伪造逐文件摘要、缺失依赖、跨机器重汇总、缺段/断段、段 P95 被总体稀释及“各段各自合格但 200≠总体 240”的嵌套遥测伪造；`node tools/test_summarize_ai_performance_baseline.mjs`、`node tools/test_analyze_force_expert_ablation.mjs` 于 0901 退出 0，并已纳入 EVID-9b 冻结提交的 38/38 默认统一验证。
- 边界：这是本机的可审计完整性，不是远程可信硬件或身份认证；历史 statefix 报告和旧 checkpoint 缺少 v3 provenance，只能作诊断，不能生成正式性能回执。**该子门不启动新评测，不改变性能失败、强度、发布或 expert 默认结论。**

### EVID-6：盲评全题灾难门（本地代码门已关闭）

- `summarize_blind_eval.mjs` 已升级为 `guandan-blind-eval-summary-v3`。manifest 的 `scenarioIds` 必须是非空、唯一字符串数组，且 `selectedScenarios` 精确等于数组长度；灾难复核仍要求每题同时提供 expert/proposed 布尔值。
- 权威灾难统计完全脱离玩家选择：对 manifest 全部题目分别累计两臂灾难数，共用 `scenarioDenominator`；门禁直接比较未舍入原始计数 `proposed <= expert`。被选侧的 `selectionDiagnostic` 仅作诊断，不参与 gate；复核不完整时正式两率为 `null` 且拒绝通过。
- 回归覆盖完整通过、缺一题复核、玩家翻转为 expert 但未选 proposed 仍为灾难、manifest 重复 ID 和数量不一致；`node tools/test_blind_eval_tools.mjs` 于 0901 退出 0，目标文件语法和目标差异检查通过，并已纳入 EVID-9b 冻结提交的 38/38 默认统一验证。独立最终审查 PASS（针对此前版本）。
- 边界：该门只证明盲评回执的统计完整性，不代表真人收益门、强度门或发布门通过；不改变 expert 默认，也不把历史 v2 summary 当作当前正式回执。

### EVID-7：fxe 契约收紧（本地代码门已关闭）

- `tools/analyze_force_expert_ablation.mjs` 现在只接受完整 `guandan-ai-ab-checkpoint-v3`。bootstrap 前逐层校验顶层字段、`signature`/`signaturePayload`、`checkpointIntegrity.sha256`、完整 provenance/运行段环境、`complete=true`、`failures=[]` 和精确 `nextBlockIndex=groupCount`。
- 两臂固定为 `ismcts-v3` / `ismcts-v3-fxe` 对 `expert`，显式 `evaluationOpponentModelMode=off`；`evaluationImplementationSha256` 必须是合法且两臂相同的摘要；除 `candidate` 外的签名载荷、当前 resolver 的确定性 v3 搜索配置、区组/级别/种子设计必须完全一致。
- 结果层严格覆盖 seed×level×team，验证同一基础区组的跨级牌面、镜像 pair↔game、运行段归属、牌面指纹、先手、升级与双上派生；正常臂逐对象要求 `changed === wouldChange`，fxe 强制 `changed=0`，并检查控制臂专家等价。
- 回归命令：`node tools/test_analyze_force_expert_ablation.mjs`（含 legacy v1/v2、缺字段、篡改、重复/缺失、跨臂牌面指纹/先手/源码摘要负例）于 0901 退出 0；两份目标文件 `node --check` 和目标差异检查通过，并已纳入 EVID-9b 冻结提交的 38/38 默认统一验证。
- 边界：该门只关闭 fxe 分析器的本地契约假绿路径；它不证明正常臂强度或 search-triggered 性能，不替代 EVID-8 发布验证，也不解除当前批次性能失败后禁止继续 fxe 的停止线。expert 默认保持不变。

### 状态隔离后 v3 性能探针

- 文件：`data/eval-20260830-ismcts-v3-statefix-probe10.json`。
- 260 局 / 130 镜像组 / 10 区组，0 失败、死锁、镜像不一致。
- 762 个 search-triggered 回合，P95/P99 `340.9/416ms`，探针性能通过。
- 收益 `-0.019/局`，CI `[-0.073,+0.035]`；只用于性能与状态隔离诊断。
- 报告 SHA-256 `0b4469c6df3628aed3cbae2142a5df5b2a5bde45f712b2dbf975f08fafa335c3`；checkpoint SHA-256 `3d577e44937188d781e0e34a022c16323880a852200234ab72f407395d430927`。

### 状态隔离后 v3 正式正常臂

- 文件：`data/eval-20260830-ismcts-v3-statefix-normal-ab80.json`；种子 `20265401–20265480`。
- 2,080 局 / 1,040 镜像组 / 80 区组，0 失败、死锁、镜像不一致；checkpoint `complete=true`。
- 收益 `+0.028/局`，按 80 个基础牌区组独立复算 bootstrap 95% CI `[+0.005,+0.051]`。
- 6,677 个 search-triggered 回合；整体 P95/P99 `619/1022ms`，严格性能失败。
- 分段复算：区组 1–60 共 5,058 个触发回合，P95/P99 `421.8/550ms`；区组 61–80 共 1,619 个触发回合，`942/1481ms`。阶跃边界与外部运行日志中的人工 resume 记录对齐，但 v2 checkpoint 没有内生运行段 provenance，不能据此作因果归因；慢段仍不得删除或只采用前段。
- 报告 SHA-256 `3b4ebd543d56af0d9c860eef1c07d5fc294b3817a7da006612f3b0e04161762a`；checkpoint SHA-256 `1a8ed1de8a4a67d6b474bd0bbf1a16f38cebcbff2f1294edd12c91aa26f9a887`。
- 报告列出的 12 个源码文件与当前工作区逐项哈希一致，但指纹清单遗漏实际依赖，因此只称“当前快照诊断”，不称完整依赖闭包证明。
- 结论：**性能门失败；不运行同批 `ismcts-v3-fxe`，不以正 CI 宣称晋级。**

### 种子隔离

以下均已用于旧评测、调参、烟雾、探针或正式失败运行，未来不得作为未见正式种子复用：

- `20261001–20261080`；
- `20262101–20262110`、`20262201–20262210`、`20262301–20262310`；
- `20263001–20263080`、`20264101–20264180`、`20264202`；
- `20265001–20265010`、`20265101–20265180`、`20265201`；
- `20265301–20265310`、`20265401–20265480`；
- `20268101–20268102`（STRAT-6 smoke，已消费）、`20268103–20268110`（smoke/正式间缓冲，不运行）、`20268111–20268150`（STRAT-6 正式三臂，已消费）、`20268151–20268190`（正式后缓冲，不运行）；登记与禁用校验见 `tools/strat6-seed-registry.json`。

## 五、明确不做

- 不放宽 `500/750ms` 或正 CI 下界门槛，不裁掉慢段，不用探针收益替代正式强度证据。
- 不在正常臂任一硬门失败后继续同批 fxe，不反复换种子直到出现想要的结果。
- 不把外部隔离轨迹直接用于训练，不新增外部扩采。
- 不在 DMC 契约硬化、Python 规则差分和 10 万条零差异前置未完成时启动正式 DMC/DanZero 长训。
- 不把本机进程存活、checkpoint 存在、测试全绿或文档勾选当作发布完成。
