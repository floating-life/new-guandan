# 掼蛋训练大师：审计结论与项目 TODO

> 审计日期：2026-08-28
> 当前分支：`main`（HEAD 与 `origin/main` 一致）
> 工作区：16 个已跟踪文件被修改，26 个未跟踪项，共 42 项未提交变更
> 当前发布结论：**专家策略版暂不可发布；价值模型不得晋级。**

## 1. 执行原则

- 分成两条路线推进：
  - **A. 稳定专家版发布线**：先恢复全绿回归、浏览器验收和可复核交付。
  - **B. 价值模型晋级线**：修复发布证据绑定，完成 M3，但不得阻塞 A 线。
- `promoted` 不是手工填写的状态；任何权重、评测报告或发布回执无法互相校验时，一律保持专家默认。
- 小样本只作 smoke，不得表述为“强度提升”。主 A/B 的 95% 置信下界必须大于 0 才能晋级。
- 外部回放继续隔离；未取得授权、未完成标签与分布审计前，`trainingEligible` 必须保持 `false`。
- 每项任务只有在“验收标准”全部满足并记录证据后，才能勾选完成。

## 2. 当前进度快照

| 工作流 | 当前状态 | 已有证据 | 下一道门 |
|---|---|---|---|
| 稳定专家版 | 🟢 交付基线已闭环 | 默认统一验证 25 项全绿；模型持久化失败不再改变运行时模型；HTTP 浏览器核心路径与远端 CI 均已实测 | 进入专家版发布候选验收 |
| M0 数据/发布安全 | 🟡 防误配/篡改已闭环 | 模型权重语义摘要、发布回执绑定、种子清单、外部数据隔离、模型状态门禁已实现 | 如需抵御伪造整份 JSON，另加签名或内置摘要信任根 |
| M1 工程化验证 | 🟢 本机与远端 CI 均已通过 | `tools/verify.ps1 -FullData` 已验证 `20260901` 数据、两份模型、A/B 和连续赛检查点；默认 25 项远端 CI 已绿 | 后续按分层计划补夜间 FullData 与性能基线 |
| M2 自对弈与正式 A/B | 🟡 历史证据已完成 | 40 副、3,859 条轨迹；1,040 局/520 镜像组；0 故障 | 旧报告采用文件摘要口径；新语义摘要候选必须重跑主 A/B，且原 CI 下界跨零 |
| M3 发布质量 | 🟡 连续赛已完成 | 新 `root-pimc-v1` 连续赛 8/8 场、0 故障，贡还/长局覆盖均达标 | 当前主 A/B 仍需按语义摘要重评并获得正向置信下界；浏览器试玩、性能基线待补 |
| 外部训练数据 | ⚪ 隔离候选 | 严格轨迹 20,409 条、适配轨迹 4,086 条；验证均通过 | 授权、规则重放标签、暗牌边界和 OOD 审计；当前可训练数仍为 0 |

### 已核验的关键数字

- 默认统一验证已实际跑完并返回 0（25 项检查）；价值模型相关测试为：Hybrid 35、门禁 22、Worker 9、Worker 集成 3，均为 0 failed。
- 原旧版 `promoted` 测试夹具已替换为依据实际网络层构造的完整有效回执；生产门禁没有放宽。
- 当前 `20260901` FullData 已实际跑完并返回 0；A/B 报告核对为 1,040/1,040 局、520/520 镜像组、训练/评测种子无重叠、0 失败/死锁/镜像偏差。
- 历史 A/B 报告使用旧的“模型文件字节摘要”口径，现已被明确标识为 `legacy_file_v1`，可审计但 `releaseCompatible=false`；新 A/B 使用跨运行时的语义权重摘要。
- `data/selfplay-20260901.jsonl` 严格校验通过：40 副、3,859 条轨迹、7,886 个候选。
- 正式 A/B：1,040/1,040 局、520/520 镜像组、0 失败/死锁/镜像偏差；升级效用 `+0.010/局`，bootstrap 95% 为 `-0.008～+0.029`，**下界跨 0**。
- 历史连续赛 smoke 有 2 场；2026-08-28 已新建语义摘要绑定的 `root-pimc-v1` 连续赛：8/8 场、4/4 镜像组、0 失败/死锁/镜像偏差，113 轮、92 个贡还轮、11 个 ≥120 动作长轮。

## 3. 项目关键流程

| Gate | 进入条件 | 输出 | 当前状态 |
|---|---|---|---|
| G0 变更收口 | 盘点 42 项改动，按主题拆分 | 可审查的提交序列 | 已完成：4 个功能主题提交，另有独立 CI 修复提交 |
| G1 工程基线 | 默认验证全绿；FullData 验当前工件 | 可信的本机与远端基线 | 已完成：远端 verify #2 成功 |
| G2 专家版候选 | G1 通过；浏览器、恢复、性能、隐私验收通过 | 专家策略发布候选 | 未开始 |
| G3 模型证据 | 权重绑定修复；主 A/B、逐级、连续赛证据完整 | `validated` 或 `promoted` 回执 | 进行中 |
| G4 模型晋级 | G2、G3 均通过且主 A/B CI 下界 > 0 | 可由网页加载的模型 | 未满足 |

推荐提交顺序：`安全门禁与测试夹具` → `统一验证/CI` → `搜索实现` → `数据工具` → `UI与文档`。不要把当前 42 项改动压成一个不可审查的大提交。

## 4. P0：先恢复可信基线

### [x] T0-1 修复价值模型测试夹具漂移

- 范围：`js/ai.hybrid.test.js`、`js/ai.worker.test.js`、`js/ai.worker.integration.test.js`。
- 动作：抽取或复用一份与 `js/value-model-gate.test.js` 一致的完整有效回执，补齐逐级质量和连续赛字段。
- 禁止：通过降低 `hasPromotionReceipt` 条件让旧夹具重新通过。
- 验收标准：
  - 三个测试文件均为 0 failed；
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File .\tools\verify.ps1` 完整跑到底并返回 0；
  - “validated 不能替换已激活 promoted 模型”和“无效模型不破坏旧模型”仍有独立断言。

已于 2026-08-28 完成：抽取 `js/value-model.test-fixture.js`，以真实模型层和完整 M3 回执生成夹具；三个目标测试及默认统一验证均为 0 failed。

### [x] T0-2 把发布回执绑定到实际权重

- 问题：运行时目前验证回执字段，却没有重算并核对当前模型权重摘要；复制一份完整回执到被修改的权重上仍可能被加载。
- 范围：`js/value-model-gate.js`、`js/ai-hybrid.js`、`tools/promote_value_model.mjs`、A/B 报告生成与相关测试。
- 动作：
  1. 定义稳定的 canonical model payload（至少包含 schema、层结构、weights、bias、activation）和摘要算法；
  2. 主 A/B、连续赛、发布工具和浏览器加载使用同一摘要口径；
  3. 发布回执记录并校验模型摘要、主 A/B 报告摘要、连续赛报告摘要；
  4. 若目标是抵御用户伪造整份 JSON，而不只是防止误配/篡改，增加可信公钥签名或内置模型摘要白名单；普通自填哈希不构成信任根。
- 验收标准：
  - 改动任意一个权重、bias 或 activation 后加载必被拒绝；
  - 把另一模型的合法回执移植过来必被拒绝；
  - 原始合法晋级工件在主线程和 Worker 中均可加载；
  - 错误加载不会破坏上一份已激活模型。

已于 2026-08-28 完成防误配/篡改范围：新增 `js/model-fingerprint.js`，以 canonical payload SHA-256 绑定权重、bias、activation 和层结构；发布工具、A/B 报告、主线程和 Worker 采用同一口径。门禁测试已覆盖“改一个权重”和“移植另一模型回执”均被拒绝。该机制不把可编辑 JSON 本身当作信任根；若威胁模型包含伪造整份 JSON，仍需单独决策并实现签名或内置摘要白名单。

### [x] T0-3 修正统一 FullData 验证目标

- 问题：`tools/verify.ps1 -FullData` 仍硬编码 `20260827` 数据/模型，不验证当前 M2 的 `20260901` 数据、模型、正式 A/B 和连续赛检查点；缺文件时只打印 skip 也会成功。
- 动作：
  - 支持显式 `-Dataset`、`-Model`、`-ABReport`、`-ContinuousReport` 参数，或由受版本控制的 manifest 指定当前工件；
  - “声明要验的文件不存在”必须失败；本机可选工件才允许明确 skip；
  - 默认 CI 保持不依赖被 `.gitignore` 排除的大文件；另设本机 FullData/夜间验证。
- 验收标准：
  - 当前 `20260901` 数据和 experimental/gated 模型实际被校验；
  - 报告中的模型摘要、训练/评估种子不重叠、局数、镜像数、失败数均被核对；
  - 删除一个声明的必需工件时命令非零退出。

已于 2026-08-28 完成：`-FullData` 改为可传入且默认指向 `20260901` 的数据、实验模型、gated 模型、主 A/B 报告和连续赛检查点；必需工件缺失立即失败。新增证据审计会核对模型摘要、报告绑定、训练/评测种子、局数、镜像数和安全计数。本机 FullData 返回 0；现有连续赛检查点仍为 1/4 组，尚未完成发布质量。

### [x] T0-4 修正文档中的高风险命令

- 问题：README 示例先用 `20260901–20260940` 训练，却用 `20260910` 起跑 40 个 A/B 种子，重叠 31 个种子；长跑结束后会被未见种子门禁拒绝。
- 动作：
  - 正式 A/B 示例改用已验证的不重叠范围（如 `920000–920039`）；
  - 长跑前增加 seed manifest overlap preflight，重叠时在第一局前失败；
  - 晋级命令补 `--continuous-report=...`；
  - 输出命名与 `.gitignore` 对齐，例如仅 `*.promoted.json` 可进入发布评审。
- 验收标准：复制 README 命令不会产生种子重叠；缺连续赛报告不能得到 `promoted`。

已于 2026-08-28 完成：README 正式 A/B 示例改为 `920000` 未见种子；启动前会显式拒绝与训练种子重叠的评测。已用 `920100` 不重叠种子完成 2 局镜像 smoke（0 故障）；晋级示例包含连续赛报告。

## 5. A 线：稳定专家版发布

### [x] T1-1 收口工作树并拆分提交

- 先保存当前 `git status` 和验证结果，不覆盖已有用户改动。
- 建议至少拆为：
  1. 对手模型与统计；
  2. 混合/ISMCTS 搜索与 Worker；
  3. 价值模型门禁/训练链路；
  4. 外部数据工具；
  5. CI、README 和计划文档。
- 验收标准：每个提交可独立解释、`git diff --check` 通过，代码与测试同提交。

2026-08-28 已完成：变更按主题拆为 `4d232c0`（公平搜索、本地模型门禁与测试）、`48f6e1b`（可复现实验模型证据工具）、`42e9600`（外部数据隔离工具）和 `26925d0`（CI、验证入口与文档）；远端编码修复独立为 `25603dc`。提交前后均执行 `git diff --check`，代码与相应测试同提交；工作树已清洁。

### [x] T1-2 让 CI 真正成为交付证据

- 动作：提交 CI；固定 Node/Python 版本；启用依赖缓存不是必需条件，先保证可复现。
- 后续分层：
  - PR：默认快速回归；
  - 夜间/手动：FullData、连续赛 smoke、性能基线；
  - 发布：浏览器验收清单、制品摘要和人工批准。
- 验收标准：远端 push/PR 实际出现一次全绿运行，并保存运行链接或编号。

2026-08-28 已完成：Windows runner 固定 Node 22、Python 3.12，最小只读权限，20 分钟上限、同分支串行和手动触发均已配置；它调用与本机一致的 `tools/verify.ps1`。首次运行 #1 暴露了 Windows `cp1252` 输出中文诊断导致的 `UnicodeEncodeError`，已由独立提交 `25603dc` 显式设置 `PYTHONUTF8=1` 和 `PYTHONIOENCODING=utf-8` 修复。远端 [verify #2](https://github.com/floating-life/new-guandan/actions/runs/33166404102)（提交 `25603dc`）已成功，成为默认 25 项校验的首个绿灯证据。

### [x] T1-3 增加真实浏览器 E2E 验收

- 最少覆盖：启动页、开始一局、合法出牌/过牌、还贡、刷新恢复、复盘、统计导入导出、三种本地引擎切换、未晋级模型拒绝、localStorage 配额失败。
- 覆盖桌面和手机断点；检查键盘焦点、Esc、状态播报、减少动画。
- 验收标准：在实际 HTTP 服务中通过，不以静态字符串测试代替浏览器行为。

2026-08-28 已完成三轮实际 HTTP 验收（独立端口，未读取或改动既有对局数据）：新局启动、电脑首轮、合法 4 炸选择/出牌、评价更新、刷新后恢复、统计与复盘弹窗、Esc 关闭弹窗、大师模式下 expert/hybrid/ismcts 三种引擎切换，以及 390px 手机断点无横向溢出均通过。第二轮用页面显示的合法推荐完成一副完整对局（11 次出牌、10 次过牌），核对终局名次/升级/余牌公开和“查看本副复盘”；下一副实际进入贡还，页面仅开放合规还贡牌，选中后“确认还贡”才启用，确认后恢复正常电脑回合。第三轮在新的隔离端口验证：统计 JSON 导出实际落盘，内容仅含版本、统计、设置和复盘（不含价值模型或进行中牌局）；可控的 7 副统计 JSON 成功导入且页面立即显示；`value-model-20260901-experimental.json` 通过真实文件选择器上传后被拒绝，状态保持“未加载晋级模型”。模型持久化配额失败已由 T1-4 的独立回归覆盖。

### [x] T1-4 修复模型持久化失败后的状态不一致

- 问题：UI 先激活模型再写 `localStorage`；若配额不足，页面提示加载失败，但主线程/Worker 中模型可能已经生效。
- 动作：先可靠持久化再激活，或保存失败时回滚上一模型并刷新状态；明确本次会话临时启用是否允许。
- 验收标准：模拟 `QuotaExceededError` 后，UI、主线程、Worker、持久化状态四者一致。

已于 2026-08-28 完成：模型先做纯校验、后持久化、再同步启用；存储写入失败时运行时启用函数不会被调用，四处均保留上一模型。若运行时启用被拒绝，持久化状态会回滚。`js/value-model-persistence.test.js` 模拟配额失败、运行时拒绝和成功路径，7 项均通过。

### [ ] T1-5 完成专家版人工发布验收

- 运行默认验证与当前工件 FullData 验证。
- 人工完成至少 3 副：普通升级、贡还、打 A/不过 A 场景各有覆盖。
- 记录专家/混合/信息集引擎的平均与 P95 决策耗时、UI 卡顿、超时回退次数。
- 本地 AI 模式确认不发起云端请求；云端模式确认只经本机代理，且不泄露 API Key。
- 验收标准：没有 P0/P1 未关闭问题，形成版本号、变更摘要、已知边界和回滚方法。

## 6. B 线：搜索与价值模型晋级

### [x] T2-1 明确信息集搜索的真实算法口径

- 问题：根搜索完成“候选 × 假想世界”基础覆盖后，后续 UCB 访问落到已评分世界，只增加 attempts/iterations，不产生新 rollout；现状更接近“成对 PIMC + 置信门”，与 README 的“UCB 分配后续模拟预算”不一致。
- 二选一：
  - **实现真实增量搜索**：UCB 选择后产生新的独立 determinization 或有明确随机种子的树内扩展；只有独立样本可缩小标准误；
  - **诚实降级命名**：改名为“成对根 PIMC”，删除空转预算和 UCB 强度表述。
- 验收标准：iteration 增加时，要么实际模拟节点/独立样本增加，要么不再声称 UCB 搜索；测试不得把“空计数”当成算法成果。

2026-08-28 已完成“诚实降级命名”分支：实验策略统一改为“成对根 PIMC”，新 A/B 候选名为 `root-pimc-v1`；每个候选仅在同一公开信息世界中实际 rollout 一次，`iterations` 精确等于这些真实 rollout 数，已删除会重复访问已评分世界的 UCB 空转循环。`ismcts-v1` / `ismcts-root-v1` 仅保留旧设置、旧报告和旧复盘的读取兼容，并在运行遥测中归一为新名称。发布门禁还新增“主 A/B 与连续赛必须为同一候选策略”的核对（旧别名按同一成对根 PIMC 策略等价）。`ai.hybrid`、规则、统计、UI 静态和模型门禁回归均通过。

### [x] T2-2 完成正式连续赛检查点

- 当前检查点：`baseSeed=931000`，计划 4 镜像组/8 场，已完成 1 组/2 场，`complete=false`。
- 从现有检查点恢复剩余 3 组，不重跑已完成组；种子必须与训练集、主 A/B 都不重叠。
- 验收标准：8/8 场、4/4 镜像组、0 失败/死锁/镜像偏差；至少覆盖 1 个贡还轮和 1 个 ≥120 动作长轮；报告摘要进入发布回执。

2026-08-28 已完成。语义摘要上线后，旧 `ismcts-v1` 检查点仍以旧文件摘要签名，不能与新 `root-pimc-v1`/语义摘要报告混合续跑；因此保留旧工件，新建 `data/value-model-20260901-root-pimc-continuous.{json,checkpoint.json}`。新连续赛使用 `baseSeed=931000–931003`（与训练和主 A/B 均不重叠），完成 8/8 场、4/4 组、0 失败/死锁/镜像偏差；覆盖 113 轮、92 个贡还轮、11 个 ≥120 动作长轮。独立连续赛质量门返回 `ok=true`；它是连续性/安全证据，不与旧主 A/B 拼接成晋级回执。

### [x] T2-3 不以当前模型申请晋级

- 当前模型主 A/B 置信区间为 `-0.008～+0.029`，即使连续赛完成，也只能维持 `validated`。
- 在完成 T2-1 后，再决定是否：
  - 扩大干净自对弈数据并重新训练；
  - 调整特征/目标函数；
  - 使用新的、不重叠种子完整重评。
- 验收标准：只有新的正式主 A/B 95% 下界严格大于 0，且全部 M3 安全门通过，才生成 `promoted`。

2026-08-28 已明确不申请晋级。当前语义模型摘要为 `e708…9db1`；历史 1,040 局主 A/B 绑定的是旧文件摘要 `ab9d…0354`，门禁如实返回 `experimental_unvalidated / model_hash_mismatch`，不能拿旧报告为当前语义模型背书。即使按历史口径看，主 A/B 95% CI 仍是 `-0.008～+0.029`，下界不为正；新连续赛通过也不能改变该结论。未生成 `promoted` 文件，网页默认专家策略保持不变。

### [ ] T2-4 增加模型性能与真人牌局门禁

- 性能：记录候选数、采样数、真实 rollout、节点数、平均/P95/P99 延迟、超时回退率。
- 真人牌局：建立盲评表，区分“专家一致”“模型改对”“模型改错”“证据不足拒绝改选”。
- 验收标准：模型在真实关键局面有足够改选覆盖，且不能靠几乎从不改选获得表面安全。

## 7. 数据工程与外部回放

### [ ] T3-1 把外部大文件校验改为流式

- 问题：`validate_external_replay_policy.mjs` 和转换器使用整文件读取、`split/map` 与整体序列化；当前约 366 MB 轨迹在 512 MB Node 堆限制下可 OOM。
- 动作：逐行解析 JSONL、增量统计、临时文件写入、成功后原子替换；错误保留行号与最多 100 条摘要。
- 验收标准：使用 `node --max-old-space-size=512` 可完成当前轨迹校验；峰值内存随文件大小近似常量。

### [ ] T3-2 建立外部数据准入决策记录

- 明确来源许可、可否再分发、可否用于模型训练/商业使用、删除请求处理方式。
- 完成 acting-seat 标签、名次符号、规则版本、暗牌白名单和 train/validation/held-out 按来源对局分组审计。
- 验收标准：在上述内容未全部通过前，所有外部轨迹继续 `trainingEligible=false`；不得改标签绕过。

### [ ] T3-3 校正文档现状

- `PROJECT_EXECUTION_PLAN.md`：M3 从“待实施”改为“门禁代码已实现，证据进行中”。
- README：服务不仅有静态资源和 `/healthz`，还包含本机 LLM 配置/健康/决策 API；修正“仅开放”的绝对表述。
- 写清：当前没有 `promoted` 模型，专家策略是预期默认，不是异常回退。
- 验收标准：README、执行计划、代码路由和验证命令相互一致。

## 8. 建议排期

| 阶段 | 建议时间 | 必做任务 | 退出条件 |
|---|---:|---|---|
| S0 基线抢修 | 0.5–1 天 | T0-1、T0-3、T0-4 | 默认验证全绿，FullData 不再验错工件 |
| S1 安全与交付 | 1–2 天 | T0-2、T1-1、T1-2、T1-4 | 权重/回执绑定可靠，CI 有远端绿灯 |
| S2 专家版候选 | 1 天 | T1-3、T1-5、T3-3 | 浏览器、性能、隐私、恢复验收通过 |
| S3 模型研究 | 视算力 | T2-1、T2-2、T2-3、T2-4 | 新证据决定 validated/promoted，不影响专家版 |
| S4 数据扩展 | 1–3 天起 | T3-1、T3-2 | 大文件可扩展，授权与准入决定留痕 |

## 9. 本轮验收记录

- [x] `git status`、文件结构、README、执行计划、CI/验证入口已审计。
- [x] `git diff --check` 和 JS/MJS/Python/PowerShell 语法阶段通过。
- [x] 默认统一验证已实际运行，并确认在 hybrid 测试失败而非“全绿”。
- [x] 失败后的其余核心测试已单独补跑，确认失败集中在 3 个模型夹具文件。
- [x] 当前 `20260901` 自对弈数据、实验模型、Python 导入器、外部隔离轨迹已实际校验。
- [x] 全程保留审计前已有工作区改动；本轮新增的门禁、夹具、验证、持久化和文档改动均在本 TODO 中留有验收记录。
- [x] T0-1：价值模型测试夹具已与生产回执同步；加入持久化回归后，默认验证返回 0（25 checks）。
- [x] T0-2：模型语义摘要和回执绑定已实现；修改权重或移植回执的回归用例均通过。
- [x] T0-3：`pwsh -NoProfile -ExecutionPolicy Bypass -File .\tools\verify.ps1 -FullData` 返回 0，实际核对当前 `20260901` 证据包。
- [x] T0-4：README 命令、种子 overlap preflight、连续赛晋级命令与现状已对齐；不重叠种子 smoke 已通过。
- [x] T1-1：工作树按公平搜索/模型证据/外部数据/CI文档拆为 4 个主题提交，另有独立 CI 编码修复提交；提交前后 `git diff --check` 均通过。
- [x] T1-2：远端 [verify #2](https://github.com/floating-life/new-guandan/actions/runs/33166404102) 成功；首次失败的 Windows 控制台编码问题已由 `25603dc` 修复并复验。
- [x] T1-4：模型先持久化后启用；模拟浏览器空间不足时，持久化、主线程和 Worker 均保持上一模型（7 个回归通过）。
- [x] T2-1：实验搜索改为诚实命名的成对根 PIMC；删除 UCB 空转，iteration 只计真实 rollout，旧 `ismcts` 名称仅作兼容。
- [x] T2-2：新 `root-pimc-v1` 连续赛 8/8 场、0 故障，贡还与长局覆盖均达到门槛；未与旧摘要检查点混合。
- [x] T2-3：连续赛通过不改变主 A/B 下界跨零及旧摘要不兼容的事实；没有生成或加载 `promoted` 模型。
- [x] T1-3：实际 HTTP 浏览器已覆盖新局、合法出牌/过牌、完整一副、终局复盘、贡还、刷新恢复、三种本地引擎、弹窗键盘、手机断点、统计导入/导出及未晋级模型拒绝；临时验收服务已关闭。
